/* ==========================================================================
   RELENTLESS FRAMES — site runtime
   Hash router, home filmstrip carousel, drawer, search, reveals.
   ========================================================================== */
(function () {
  'use strict';

  var MARK = "<svg preserveAspectRatio=\"xMidYMid meet\" xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"0 0 1271.9 1139.1\" fill=\"currentColor\"><path fill-rule=\"evenodd\" d=\"M679.5 1135.7C691.2 1130.1 708.2 1114.9 725.0 1094.7C733.2 1084.9 739.5 1075.5 747.7 1061.3C748.6 1059.8 750.4 1056.8 751.7 1054.8C753.0 1052.9 754.7 1049.4 755.5 1047.2C756.3 1045.0 757.7 1042.3 758.8 1041.2C759.8 1040.1 761.1 1037.4 761.7 1035.2C762.3 1033.0 763.4 1030.3 764.2 1029.1C765.1 1027.9 766.3 1024.9 766.9 1022.3C767.6 1019.6 769.1 1016.3 770.3 1014.8C772.6 1012.1 778.2 995.4 779.4 987.8C779.7 985.4 781.4 980.6 782.9 977.3C784.5 973.9 785.8 970.0 785.8 968.6C785.8 967.2 786.8 962.5 787.9 958.1C790.9 946.9 791.9 932.1 792.8 885.7C793.5 846.3 793.6 845.0 796.1 836.2C797.5 831.2 799.9 825.0 801.3 822.3C802.7 819.6 803.8 816.8 803.8 816.1C803.8 814.9 812.1 798.4 813.7 796.2C814.2 795.7 815.3 792.8 816.3 790.0C817.2 787.1 818.4 784.5 818.9 784.2C819.4 783.9 820.3 781.7 820.9 779.4C821.5 777.1 823.0 774.1 824.3 772.7C825.6 771.3 826.9 769.1 827.3 767.7C828.8 762.7 834.9 749.4 837.2 746.3C838.5 744.6 839.9 742.2 840.3 741.0C841.7 736.2 848.0 727.1 852.4 723.5C855.0 721.4 857.4 719.7 857.8 719.7C858.2 719.7 859.7 718.6 861.2 717.2C864.3 714.4 876.8 708.2 881.5 707.2C883.3 706.9 885.8 705.7 887.1 704.7C890.1 702.4 899.0 699.1 909.3 696.5C913.7 695.4 919.1 693.6 921.3 692.6C923.5 691.5 927.8 689.9 930.8 689.2C933.9 688.4 939.5 686.7 943.3 685.3C947.2 683.9 951.7 682.7 953.3 682.7C955.0 682.7 958.0 681.8 960.1 680.7C962.1 679.6 965.9 678.2 968.4 677.7C971.0 677.1 974.9 675.8 977.2 674.8C979.5 673.7 985.8 672.1 991.3 671.2C996.7 670.3 1002.8 668.7 1004.8 667.7C1006.7 666.8 1010.8 665.7 1013.7 665.3C1016.7 664.9 1020.8 663.7 1022.8 662.7C1024.8 661.7 1029.3 660.3 1032.9 659.7C1036.4 659.1 1042.7 657.6 1046.8 656.5C1051.0 655.3 1056.8 654.0 1059.7 653.5C1062.7 653.0 1067.2 651.7 1069.7 650.6C1072.3 649.6 1079.3 647.8 1085.3 646.7C1104.9 643.2 1117.8 640.1 1119.6 638.4C1120.5 637.6 1123.2 636.1 1125.6 635.2C1130.7 633.1 1133.8 629.5 1133.8 625.5C1133.8 622.5 1127.7 615.3 1123.1 612.8C1118.0 610.1 1083.4 612.1 1074.4 615.6C1072.7 616.3 1066.7 617.5 1060.9 618.3C1055.1 619.1 1047.6 620.5 1044.3 621.3C1041.0 622.2 1035.6 623.3 1032.3 623.7C1028.5 624.1 1024.8 625.3 1022.3 626.9C1019.9 628.5 1016.1 629.7 1012.7 630.1C1009.6 630.5 1003.1 631.9 998.2 633.2C993.3 634.6 988.4 635.7 987.2 635.7C986.1 635.7 982.7 637.0 979.7 638.6C976.8 640.1 972.3 641.8 969.8 642.2C967.4 642.6 962.6 643.9 959.3 645.2C956.0 646.4 951.1 647.7 948.3 648.1C945.6 648.6 941.5 649.9 939.3 651.2C937.1 652.5 933.3 653.8 930.8 654.2C928.4 654.6 925.1 655.5 923.7 656.3C922.2 657.0 917.2 658.5 912.5 659.5C907.8 660.6 902.7 662.4 901.3 663.6C899.8 664.7 897.4 665.7 896.1 665.7C894.7 665.7 891.0 666.9 887.8 668.3C880.7 671.5 877.4 671.2 877.0 667.5C876.8 666.0 877.4 663.4 878.3 661.8C879.2 660.1 880.3 656.4 880.8 653.6C881.4 650.9 882.9 647.2 884.2 645.4C885.5 643.6 887.1 640.5 887.8 638.5C888.4 636.5 889.6 634.3 890.3 633.7C891.1 633.0 892.4 630.1 893.3 627.2C894.2 624.2 895.3 621.5 896.0 621.1C896.6 620.7 898.1 618.1 899.4 615.2C900.7 612.3 902.2 609.6 902.7 609.2C903.3 608.9 904.3 606.7 904.9 604.4C905.5 602.1 907.0 598.6 908.2 596.7C909.5 594.8 911.0 591.7 911.6 589.9C912.2 588.1 913.7 585.4 914.8 584.0C915.9 582.6 916.8 580.9 916.8 580.3C916.8 579.2 922.6 566.5 925.3 561.7C928.5 556.1 934.0 544.5 935.4 540.6C936.2 538.4 937.9 535.2 939.3 533.5C940.7 531.9 941.8 530.1 941.8 529.5C941.8 529.0 942.8 527.2 943.9 525.6C945.1 524.0 946.4 521.1 947.0 519.2C947.5 517.3 949.0 514.5 950.3 512.9C951.6 511.3 953.0 508.9 953.4 507.4C953.7 506.0 954.8 504.1 955.9 503.1C956.9 502.2 958.3 499.8 958.9 497.8C959.6 495.8 961.1 492.8 962.4 491.0C963.8 489.3 965.3 486.6 965.9 485.1C966.4 483.6 967.7 481.3 968.7 479.8C969.7 478.4 970.9 476.1 971.3 474.7C971.7 473.3 973.1 470.6 974.4 468.7C976.9 464.9 985.6 448.8 988.8 441.9C989.9 439.6 991.5 437.4 992.4 437.0C993.3 436.7 994.9 434.6 996.0 432.3C998.5 426.8 1003.0 419.3 1004.9 417.0C1005.8 416.0 1008.2 412.0 1010.3 408.2C1012.4 404.3 1014.9 400.3 1015.9 399.2C1016.9 398.1 1018.4 395.5 1019.3 393.5C1020.1 391.5 1021.4 389.2 1022.2 388.4C1023.0 387.6 1024.5 385.4 1025.5 383.5C1027.6 379.4 1036.5 365.8 1040.3 360.7C1041.8 358.8 1044.5 354.7 1046.4 351.7C1050.9 344.3 1056.0 337.0 1058.0 334.7C1059.0 333.6 1060.5 331.3 1061.4 329.5C1063.1 326.2 1064.5 324.2 1070.6 316.6C1072.4 314.3 1073.8 312.1 1073.8 311.7C1073.8 310.7 1080.3 301.0 1082.8 298.2C1083.8 297.1 1086.5 293.0 1088.8 289.0C1091.2 285.0 1093.7 281.6 1094.4 281.3C1095.1 281.0 1097.5 278.0 1099.7 274.5C1103.9 267.9 1107.1 263.5 1112.6 256.6C1114.4 254.3 1115.8 252.1 1115.8 251.6C1115.8 251.2 1117.1 249.3 1118.7 247.5C1120.3 245.7 1123.9 240.8 1126.7 236.7C1129.5 232.6 1133.1 227.4 1134.8 225.2C1136.4 223.0 1138.5 219.8 1139.4 218.2C1140.3 216.5 1142.1 214.1 1143.3 212.7C1146.4 209.4 1162.0 186.0 1163.6 182.3C1164.3 180.7 1165.8 178.2 1166.9 176.8C1171.3 171.2 1173.9 166.8 1177.8 158.7C1180.1 154.0 1183.3 147.5 1184.9 144.2C1187.4 139.0 1187.8 137.0 1187.8 129.9C1187.8 122.0 1187.7 121.6 1184.4 117.9C1182.5 115.8 1178.9 113.1 1176.5 111.9C1171.9 109.6 1171.9 109.6 1168.1 111.5C1166.0 112.5 1162.8 113.7 1160.9 114.1C1155.3 115.1 1149.5 119.4 1138.5 130.4C1135.9 133.0 1132.8 136.1 1131.7 137.2C1128.2 140.6 1116.5 154.4 1113.3 158.7C1111.7 161.0 1108.9 164.5 1107.2 166.5C1104.3 170.0 1102.4 172.6 1089.8 190.5C1086.8 194.8 1082.5 200.7 1080.3 203.7C1078.1 206.6 1075.4 210.5 1074.3 212.2C1073.2 214.0 1070.4 218.1 1068.1 221.3C1065.7 224.5 1060.7 231.9 1057.0 237.7C1053.2 243.5 1048.0 251.5 1045.3 255.6C1042.5 259.7 1039.2 264.9 1037.8 267.2C1034.9 272.1 1031.8 276.9 1028.9 281.2C1027.7 282.8 1025.7 285.8 1024.3 287.7C1023.0 289.6 1021.3 292.3 1020.7 293.7C1020.0 295.1 1018.5 297.3 1017.2 298.7C1015.0 301.2 1012.8 305.2 1008.8 313.7C1007.7 316.2 1006.2 318.6 1005.5 319.2C1004.2 320.2 1000.0 327.2 996.5 334.2C995.5 336.4 993.5 339.5 992.2 341.0C990.9 342.6 989.8 344.5 989.8 345.2C989.8 345.9 988.8 347.8 987.6 349.4C986.3 350.9 984.5 353.8 983.6 355.7C982.6 357.6 980.0 362.1 977.7 365.7C975.4 369.3 972.9 373.8 972.1 375.7C971.3 377.6 969.7 380.3 968.7 381.7C964.7 386.8 961.8 391.3 959.4 396.2C956.7 401.6 955.8 403.0 951.8 409.2C948.7 413.8 941.8 427.9 941.8 429.5C941.8 430.1 940.7 431.4 939.4 432.3C938.0 433.1 936.6 434.8 936.2 436.0C934.7 440.9 927.7 454.5 925.8 456.2C924.7 457.2 923.8 458.4 923.8 458.9C923.8 459.7 918.8 470.1 912.8 481.7C911.4 484.3 909.6 487.4 908.7 488.4C907.8 489.4 906.8 491.5 906.4 493.2C906.0 494.8 904.9 497.1 903.9 498.2C902.1 500.2 898.8 506.4 890.3 523.7C887.8 528.9 885.1 534.3 884.3 535.7C883.6 537.1 881.2 542.1 878.9 546.8C876.7 551.6 873.8 557.2 872.4 559.2C871.0 561.3 869.8 563.7 869.8 564.6C869.8 565.5 868.8 568.2 867.5 570.7C866.2 573.2 864.7 576.3 864.3 577.7C863.9 579.1 862.3 581.7 860.7 583.5C859.1 585.3 857.8 587.4 857.8 588.3C857.8 589.8 853.1 600.7 850.3 605.7C847.2 611.2 841.3 624.6 840.3 628.7C839.9 630.1 838.5 632.6 837.3 634.3C836.0 636.0 834.0 639.8 832.9 642.6C831.7 645.4 829.9 650.1 828.7 652.9C827.6 655.8 826.1 658.9 825.4 659.7C824.7 660.5 822.4 664.8 820.3 669.2C817.6 674.6 814.7 678.9 811.0 682.5C804.4 688.8 795.0 695.4 791.1 696.3C789.6 696.7 786.2 698.0 783.6 699.3C781.0 700.6 778.0 701.7 777.0 701.7C776.0 701.7 772.9 702.9 770.3 704.3C767.6 705.7 762.2 707.5 758.3 708.2C754.3 709.0 747.8 710.5 743.8 711.6C739.7 712.8 735.4 713.7 734.3 713.7C733.1 713.7 730.0 714.8 727.2 716.2C721.9 718.9 690.9 725.7 683.9 725.7C681.7 725.7 675.9 727.0 671.1 728.6C664.8 730.7 658.7 731.8 649.3 732.6C642.2 733.2 630.9 734.4 624.3 735.2C605.3 737.5 556.2 737.2 540.0 734.7C533.0 733.7 522.3 732.4 516.2 731.8C508.7 731.2 503.5 730.2 500.3 728.8C497.6 727.6 492.5 726.2 488.9 725.7C485.3 725.2 478.9 723.8 474.7 722.7C470.6 721.6 464.6 720.4 461.4 720.1C457.9 719.7 454.3 718.5 452.2 717.0C450.3 715.7 446.7 714.2 444.3 713.7C441.9 713.1 436.5 711.6 432.4 710.2C428.3 708.8 424.0 707.7 422.7 707.7C421.5 707.7 419.1 706.6 417.3 705.2C415.5 703.9 411.9 702.3 409.3 701.7C406.8 701.1 403.0 699.8 401.0 698.8C399.0 697.8 395.8 696.7 393.8 696.3C391.9 695.9 388.3 694.3 385.9 692.7C383.5 691.0 380.8 689.7 379.9 689.7C379.0 689.7 377.0 688.8 375.3 687.7C373.7 686.6 369.9 685.0 366.8 684.1C363.8 683.2 360.2 681.6 358.7 680.6C357.2 679.5 355.6 678.7 355.1 678.7C354.6 678.7 351.2 677.1 347.5 675.2C343.9 673.3 340.4 671.7 339.7 671.7C338.1 671.7 322.0 663.8 321.3 662.7C321.0 662.2 318.5 661.1 315.7 660.3C313.0 659.4 309.5 657.9 307.9 656.7C306.3 655.6 304.4 654.7 303.6 654.7C302.8 654.7 300.2 653.4 297.7 651.8C295.2 650.3 291.4 648.4 289.3 647.7C287.1 647.0 284.4 645.8 283.3 645.1C282.2 644.4 279.3 643.1 276.8 642.2C274.4 641.3 271.5 639.8 270.4 638.7C269.2 637.7 266.5 636.2 264.3 635.4C254.9 631.9 247.2 628.3 245.3 626.7C243.3 624.9 237.3 622.2 227.0 618.6C224.1 617.6 220.3 615.6 218.7 614.2C217.0 612.8 215.1 611.7 214.5 611.7C213.8 611.7 211.3 610.6 208.8 609.3C206.3 608.0 203.0 606.7 201.4 606.3C199.8 605.9 197.1 604.4 195.5 603.1C193.9 601.7 191.3 600.3 189.7 600.0C185.8 599.2 178.8 591.5 178.8 588.0C178.8 584.5 181.8 579.5 185.3 577.0C186.7 576.0 189.5 574.0 191.4 572.5C193.3 571.0 196.4 569.5 198.1 569.1C199.9 568.8 203.4 567.4 205.8 566.2C208.3 565.0 211.6 563.7 213.2 563.3C214.7 562.9 217.4 561.6 219.0 560.4C220.6 559.2 224.5 557.5 227.6 556.5C238.1 553.4 245.3 550.5 247.3 548.7C249.2 547.0 253.9 545.1 266.9 540.7C270.0 539.7 274.2 537.7 276.2 536.3C278.2 535.0 281.3 533.5 283.1 533.1C284.9 532.8 288.1 531.4 290.3 530.2C292.5 528.9 295.7 527.6 297.5 527.2C299.2 526.9 302.1 525.5 303.9 524.1C305.7 522.8 307.7 521.7 308.3 521.7C309.0 521.7 311.5 520.4 313.9 518.8C316.3 517.2 319.7 515.5 321.3 515.1C323.0 514.7 325.5 513.6 326.9 512.6C328.4 511.6 330.8 510.3 332.3 509.7C333.7 509.2 336.4 507.6 338.1 506.3C339.9 505.0 342.5 503.6 343.8 503.2C346.6 502.4 357.3 496.2 360.3 493.6C361.4 492.7 363.5 491.6 364.8 491.2C366.2 490.8 368.2 489.7 369.3 488.7C370.4 487.7 372.9 486.1 374.8 485.2C376.8 484.2 378.6 483.1 378.8 482.7C379.1 482.3 382.9 480.0 387.3 477.7C391.7 475.4 396.7 472.4 398.3 471.1C400.0 469.8 404.3 466.6 407.8 464.2C411.4 461.7 416.4 458.1 418.8 456.2C421.3 454.3 424.2 452.1 425.3 451.4C426.4 450.6 428.5 449.3 429.8 448.3C431.2 447.4 434.4 445.1 436.8 443.2C439.3 441.3 442.7 439.0 444.3 438.1C446.0 437.1 448.5 435.3 449.8 434.0C451.2 432.7 459.1 425.9 467.3 419.0C475.6 412.1 483.3 405.5 484.5 404.3C485.6 403.1 488.6 400.6 491.1 398.7C493.6 396.8 496.3 394.1 497.0 392.9C497.7 391.6 499.4 389.9 500.9 389.2C502.3 388.4 504.9 386.1 506.7 384.0C508.4 381.9 510.5 379.5 511.4 378.7C512.3 377.9 515.0 375.2 517.4 372.7C522.4 367.5 523.4 366.6 528.6 361.5C539.0 351.3 568.2 318.3 574.6 309.4C576.5 306.8 578.3 304.7 578.7 304.7C579.1 304.7 581.4 301.7 583.8 297.9C586.2 294.2 589.6 289.6 591.4 287.7C594.8 284.0 601.2 275.2 607.6 265.4C609.6 262.2 612.8 257.8 614.6 255.5C616.4 253.3 619.5 248.5 621.6 244.8C623.6 241.2 627.1 235.5 629.4 232.2C631.6 228.9 635.4 222.1 637.7 217.2C640.1 212.2 642.7 207.1 643.4 205.7C645.7 201.5 647.5 197.4 649.3 192.2C650.3 189.4 652.0 184.5 653.2 181.1C654.4 177.8 656.0 174.0 656.9 172.6C657.8 171.3 659.0 167.9 659.6 165.2C660.1 162.4 661.5 157.0 662.6 153.2C663.7 149.3 665.2 142.8 665.8 138.7C666.5 134.6 667.9 127.6 669.0 123.2C671.6 113.2 671.1 98.1 668.0 92.0C667.0 89.9 665.8 86.0 665.5 83.2C665.1 80.5 663.7 76.7 662.3 74.7C660.9 72.7 659.8 70.6 659.8 70.0C659.8 69.4 657.7 67.0 655.2 64.6C652.6 62.2 649.4 59.0 647.9 57.5C641.3 51.1 623.1 46.2 605.8 46.3C595.9 46.4 577.7 48.4 570.8 50.2C567.8 51.0 562.2 52.4 558.3 53.3C554.5 54.2 550.1 55.9 548.5 57.0C546.9 58.0 543.8 59.4 541.6 59.9C539.4 60.4 536.4 61.6 534.9 62.6C533.5 63.6 530.8 64.9 528.8 65.6C525.1 66.8 524.6 67.1 506.5 79.3C500.1 83.6 493.2 88.1 491.3 89.2C489.5 90.4 484.9 93.8 481.1 97.0C477.4 100.1 469.4 106.8 463.3 111.8C457.3 116.8 451.0 122.3 449.3 123.8C447.7 125.4 440.9 131.8 434.4 137.9C420.6 150.9 409.9 161.6 401.3 171.1C398.0 174.7 392.4 180.8 388.8 184.5C385.3 188.2 378.3 196.2 373.3 202.2C368.4 208.1 362.0 215.6 359.1 218.9C356.2 222.1 353.8 225.0 353.8 225.4C353.8 225.8 351.2 228.9 348.1 232.3C344.9 235.7 341.0 240.8 339.3 243.7C337.7 246.6 335.1 250.2 333.6 251.6C330.4 254.7 326.7 259.8 323.9 264.9C322.8 266.9 321.3 268.9 320.6 269.4C319.2 270.2 314.1 277.0 310.5 282.6C309.3 284.5 307.1 287.8 305.6 290.1C304.1 292.3 302.0 295.9 300.8 298.0C299.7 300.1 296.8 304.4 294.4 307.5C292.0 310.6 289.1 315.0 287.9 317.2C284.2 323.9 281.5 328.2 278.6 331.6C277.1 333.4 275.8 335.3 275.8 335.8C275.8 336.3 273.9 339.9 271.6 343.7C266.5 351.9 261.7 360.6 258.9 366.5C257.7 368.9 256.0 371.6 254.9 372.5C253.9 373.4 251.1 377.8 248.8 382.2C246.5 386.6 243.8 391.1 242.9 392.2C241.9 393.3 239.0 398.2 236.4 403.1C233.8 407.9 230.9 413.1 229.9 414.6C225.7 420.7 221.8 427.1 221.8 427.9C221.8 428.5 220.4 431.0 218.7 433.5C216.9 436.1 214.7 439.4 213.7 440.8C212.8 442.2 211.2 445.2 210.3 447.3C209.5 449.4 207.9 452.1 206.9 453.2C205.9 454.3 204.7 456.3 204.3 457.7C203.9 459.1 202.6 461.3 201.3 462.7C200.0 464.1 198.7 466.5 198.3 468.2C197.7 470.9 192.4 482.4 181.7 504.3C179.0 509.7 176.8 512.9 174.8 514.1C171.5 516.0 169.8 515.5 169.8 512.7C169.8 508.7 175.0 489.2 176.8 486.8C178.7 484.0 183.3 471.9 184.8 465.2C185.4 463.0 187.1 458.5 188.8 455.2C194.0 444.7 196.6 438.3 197.3 434.5C197.7 432.5 199.1 429.3 200.4 427.6C201.7 425.8 202.8 423.8 202.8 423.2C202.8 422.6 204.4 419.0 206.3 415.1C208.2 411.3 209.8 407.5 209.8 406.7C209.8 405.8 211.2 402.5 212.8 399.2C214.4 395.9 215.8 392.6 215.8 391.9C215.8 391.2 216.9 389.0 218.3 386.9C219.6 384.9 221.2 381.2 221.8 378.7C222.5 376.2 223.6 373.3 224.3 372.1C225.1 371.0 226.2 368.0 226.8 365.6C227.5 363.2 229.0 360.0 230.3 358.5C231.6 357.1 232.9 354.1 233.3 352.0C233.7 350.0 235.0 346.4 236.3 344.2C237.6 342.0 238.9 338.7 239.3 336.9C239.7 335.1 241.1 332.4 242.4 330.8C244.7 328.1 245.9 325.2 250.2 313.1C251.4 309.8 253.6 305.6 255.1 303.8C256.6 302.0 257.8 300.0 257.8 299.3C257.8 298.7 258.7 296.3 259.9 294.2C261.0 292.0 262.4 288.4 262.9 286.2C263.5 284.0 265.0 280.8 266.3 279.2C267.5 277.5 268.9 275.1 269.3 273.7C269.7 272.3 270.9 269.8 272.0 268.0C273.0 266.3 274.5 262.9 275.2 260.4C276.0 257.9 277.5 254.5 278.7 252.9C279.9 251.2 280.8 249.6 280.8 249.3C280.8 248.5 286.3 236.6 293.8 221.2C297.0 214.6 301.3 205.6 303.3 201.2C305.4 196.8 307.9 192.3 308.9 191.2C309.8 190.1 310.9 188.1 311.3 186.7C312.6 182.3 319.1 169.3 321.5 166.4C322.8 164.8 323.8 163.1 323.8 162.5C323.8 161.0 331.6 145.5 333.3 143.7C334.1 142.9 336.5 138.7 338.7 134.3C340.9 130.0 343.5 125.7 344.4 124.7C345.3 123.7 346.8 121.3 347.7 119.5C349.5 115.8 353.6 114.5 355.7 117.0C356.3 117.8 357.1 121.3 357.4 124.8C357.7 128.3 358.6 134.4 359.5 138.4C360.9 144.9 361.5 146.1 365.9 150.2C368.6 152.7 371.4 154.7 372.1 154.7C373.9 154.7 380.2 149.8 384.7 144.9C387.8 141.5 390.4 137.5 394.6 129.2C395.5 127.5 397.2 124.8 398.5 123.1C399.8 121.4 400.8 119.4 400.8 118.8C400.8 118.1 402.1 114.5 403.7 110.9C405.3 107.2 407.0 102.5 407.4 100.4C407.7 98.4 408.9 95.6 410.0 94.2C411.8 91.9 415.1 83.2 418.8 70.9C419.9 67.5 421.7 63.3 422.9 61.6C424.3 59.6 425.3 56.2 425.8 51.9C426.2 48.2 427.4 42.3 428.4 38.7C429.5 34.8 430.2 28.6 430.3 23.1C430.3 14.7 430.1 13.7 427.3 9.2C425.4 6.0 422.8 3.5 419.8 1.9C415.7 -0.3 414.5 -0.5 405.6 0.0C394.7 0.6 392.7 1.5 380.0 11.5C372.0 18.0 355.5 35.7 349.3 44.7C338.0 60.8 335.8 64.0 333.1 67.1C331.6 69.0 329.0 72.8 327.3 75.6C325.7 78.5 323.3 82.3 322.1 84.0C320.9 85.8 318.0 90.3 315.6 94.2C313.3 98.0 310.0 103.0 308.3 105.2C306.7 107.4 303.6 112.6 301.5 116.7C299.4 120.8 295.9 127.1 293.7 130.7C291.4 134.3 289.1 138.5 288.5 140.2C288.0 141.8 286.5 144.5 285.3 146.2C284.1 147.8 282.7 150.3 282.3 151.7C281.9 153.1 280.6 155.3 279.3 156.7C278.0 158.1 276.3 160.8 275.4 162.7C274.6 164.6 272.3 168.9 270.4 172.2C268.5 175.5 264.2 183.6 260.9 190.2C257.6 196.8 254.4 202.9 253.9 203.7C252.3 206.3 247.9 216.1 246.4 220.6C245.5 222.9 243.7 226.2 242.3 227.9C241.0 229.5 239.8 231.4 239.8 232.1C239.8 232.7 238.9 235.0 237.9 237.1C236.8 239.3 235.2 243.1 234.3 245.7C233.5 248.3 231.9 251.2 230.8 252.2C229.8 253.2 228.4 255.6 227.7 257.6C227.1 259.6 225.8 262.5 224.8 264.2C223.8 265.8 222.7 269.1 222.3 271.4C222.0 273.7 220.8 276.7 219.7 278.1C218.7 279.5 217.1 282.5 216.3 284.9C215.4 287.2 214.1 290.1 213.4 291.3C212.6 292.5 211.2 296.1 210.3 299.3C209.3 302.5 207.8 306.8 206.8 308.7C201.8 318.4 199.5 323.6 198.1 328.2C197.3 330.9 196.1 334.1 195.3 335.3C194.6 336.4 193.3 339.7 192.4 342.7C191.5 345.6 190.1 348.8 189.3 349.8C188.4 350.7 186.8 354.6 185.8 358.4C182.9 368.8 178.8 379.9 176.8 382.8C175.8 384.2 174.4 387.6 173.7 390.3C173.1 393.0 171.7 397.0 170.8 399.2C169.8 401.5 168.7 405.3 168.3 407.7C167.9 410.2 166.5 413.4 165.2 415.0C164.0 416.5 162.6 419.4 162.2 421.5C161.8 423.5 160.5 427.9 159.3 431.2C158.1 434.5 156.7 439.1 156.4 441.4C156.0 443.8 154.6 447.2 153.3 449.1C152.0 451.1 150.6 454.3 150.3 456.4C149.9 458.5 148.7 462.1 147.7 464.5C146.6 466.8 145.3 471.3 144.7 474.4C144.1 477.5 142.3 482.5 140.7 485.5C139.1 488.5 137.8 491.7 137.8 492.6C137.8 493.5 136.9 496.2 135.7 498.7C134.6 501.2 133.2 505.9 132.7 509.2C132.2 512.5 130.8 516.8 129.7 518.7C127.5 522.4 123.9 532.0 120.8 542.7C118.7 549.9 116.2 552.6 106.8 557.8C95.7 563.9 92.8 565.9 90.4 569.0C86.6 574.1 86.6 575.6 90.2 582.7C103.0 607.9 102.1 605.6 101.5 612.5C101.2 615.9 100.0 620.7 99.0 623.1C97.9 625.5 96.7 629.4 96.3 631.8C95.9 634.2 94.7 638.4 93.6 641.2C92.5 643.9 91.0 648.3 90.3 650.9C89.6 653.5 88.1 656.9 87.1 658.4C86.0 659.9 84.1 664.8 82.9 669.2C81.8 673.6 79.9 680.6 78.8 684.7C77.7 688.8 76.0 693.3 74.9 694.7C73.1 697.2 70.7 705.1 66.8 721.2C65.7 725.6 63.7 731.4 62.3 734.1C61.0 736.8 59.8 739.8 59.8 740.8C59.8 741.8 59.0 744.8 57.9 747.4C56.9 750.0 55.3 755.7 54.4 760.1C53.5 764.4 51.9 769.4 50.8 771.2C49.8 772.9 48.4 776.8 47.7 779.8C47.1 782.7 45.9 786.5 45.1 788.0C44.3 789.6 43.2 793.6 42.7 797.0C42.2 800.4 40.7 805.3 39.4 807.8C38.0 810.3 36.7 814.1 36.3 816.3C35.9 818.4 34.5 822.2 33.2 824.7C32.0 827.2 30.4 832.1 29.8 835.7C29.2 839.3 27.6 845.4 26.3 849.4C24.9 853.3 23.8 857.8 23.8 859.4C23.8 860.9 22.9 863.7 21.8 865.5C20.7 867.4 19.1 872.7 18.3 877.4C17.5 882.0 15.9 888.3 14.9 891.3C13.8 894.4 12.6 898.9 12.2 901.5C11.9 904.1 10.5 908.3 9.2 910.9C7.7 913.9 6.4 918.9 5.8 924.4C5.2 929.2 4.4 935.2 3.9 937.7C0.1 957.4 -0.3 961.6 0.0 973.8C0.3 984.8 0.6 986.9 2.4 989.3C6.0 994.2 9.4 996.1 14.6 996.1C23.4 996.0 26.0 994.4 31.6 985.9C34.4 981.7 37.6 977.0 38.8 975.5C39.9 974.1 40.8 972.0 40.8 971.0C40.8 970.0 42.4 966.1 44.3 962.2C46.3 958.4 47.8 954.6 47.8 953.7C47.8 952.9 48.8 950.6 50.0 948.7C51.1 946.8 52.7 942.7 53.4 939.7C54.9 932.8 61.3 915.0 62.9 913.1C64.0 911.8 66.3 904.4 71.1 886.7C72.2 882.6 73.9 878.2 74.9 876.9C75.8 875.7 77.4 871.2 78.3 866.9C79.3 862.7 80.5 858.4 80.9 857.5C81.4 856.6 82.5 852.6 83.3 848.6C84.2 844.7 85.7 840.2 86.8 838.7C87.9 837.1 89.3 833.7 89.8 831.0C90.3 828.4 91.6 823.9 92.7 821.2C93.7 818.4 94.9 814.2 95.3 811.7C95.7 809.2 97.4 804.8 99.0 801.8C100.5 798.8 101.8 795.6 101.8 794.5C101.8 793.5 102.8 790.8 103.9 788.6C105.0 786.3 106.4 782.0 106.9 778.8C107.4 775.7 108.9 771.1 110.3 768.4C111.6 765.8 113.0 762.0 113.4 759.9C113.8 757.9 114.9 754.8 115.9 753.2C116.9 751.5 118.6 746.5 119.7 742.0C120.8 737.4 122.4 732.7 123.2 731.5C124.0 730.2 125.0 727.4 125.4 725.2C125.8 723.0 127.1 719.4 128.4 717.2C129.6 715.0 131.1 710.8 131.7 707.9C132.3 705.0 133.9 700.0 135.3 696.7C136.7 693.4 137.8 690.1 137.8 689.4C137.8 688.6 139.0 686.3 140.4 684.2C141.7 682.2 143.4 678.2 144.0 675.4C144.6 672.5 145.7 669.1 146.4 667.7C147.1 666.3 148.3 662.5 149.2 659.3C150.0 656.1 151.6 652.5 152.6 651.3C153.7 650.2 155.1 647.1 155.7 644.5C156.3 641.9 158.1 637.6 159.6 635.0L162.5 630.2L169.0 629.9C173.0 629.7 177.1 630.2 179.4 631.1C181.6 631.8 186.3 633.6 189.9 634.9C193.6 636.2 197.8 638.3 199.3 639.5C200.8 640.7 202.6 641.7 203.2 641.7C203.8 641.7 207.0 643.0 210.3 644.7C213.6 646.3 216.9 647.7 217.5 647.7C218.1 647.7 219.9 648.7 221.5 650.0C223.1 651.3 227.9 654.0 232.3 656.1C240.7 660.0 241.2 660.2 252.4 666.5C256.3 668.6 261.2 671.0 263.4 671.7C265.6 672.4 268.0 673.5 268.9 674.3C270.8 676.0 286.0 683.7 287.3 683.7C287.9 683.7 289.9 684.8 291.8 686.1C293.8 687.3 296.5 688.7 297.8 689.2C299.2 689.6 301.7 691.0 303.4 692.2C305.1 693.5 308.5 695.0 310.9 695.7C313.3 696.3 316.3 697.5 317.4 698.2C318.5 698.9 321.5 700.1 323.9 700.8C326.3 701.5 329.5 703.0 330.8 704.3C332.2 705.5 336.0 707.5 339.3 708.7C342.6 709.9 347.8 711.7 350.8 712.8C353.8 713.8 357.4 715.5 358.8 716.4C361.3 718.2 372.7 722.4 382.3 725.2C385.0 726.0 388.5 727.5 389.9 728.6C391.4 729.7 394.8 731.1 397.5 731.8C400.1 732.5 403.5 733.5 404.8 734.2C406.2 734.9 410.3 736.1 413.8 736.7C417.4 737.4 422.8 739.3 425.7 740.8C428.7 742.4 432.3 743.7 433.7 743.7C435.2 743.7 437.9 744.5 439.8 745.5C441.8 746.6 447.7 748.1 453.1 749.0C458.4 749.9 465.5 751.8 468.8 753.2C472.1 754.6 475.8 755.7 477.1 755.7C478.4 755.7 483.2 756.8 487.8 758.2C492.4 759.6 498.1 760.7 500.5 760.7C502.9 760.7 511.4 762.0 519.6 763.7C527.7 765.3 539.7 767.1 546.3 767.7C552.9 768.2 564.0 769.4 570.8 770.3C582.3 771.8 585.7 771.8 613.8 770.2C648.2 768.4 656.7 767.6 668.8 764.8C673.5 763.7 681.8 762.3 687.3 761.6C693.1 760.9 699.8 759.4 703.0 758.1C706.2 756.8 709.8 755.7 711.0 755.6C712.3 755.6 715.1 754.9 717.3 754.0C719.5 753.2 725.8 751.2 731.3 749.6C744.5 745.9 753.9 742.6 757.0 740.5C762.9 736.6 772.2 735.6 773.8 738.6C775.5 741.8 775.0 742.9 770.0 747.6C761.7 755.4 752.6 766.0 750.7 770.2C749.7 772.4 747.9 775.4 746.8 776.8C743.9 780.3 742.9 790.0 742.5 815.2C742.2 836.7 740.5 846.5 735.3 856.7C734.0 859.2 731.7 864.1 730.0 867.7C723.3 882.4 717.6 893.7 716.8 894.2C716.3 894.5 715.2 897.0 714.4 899.8C713.6 902.6 712.0 906.3 711.0 908.0C709.9 909.8 708.7 912.3 708.3 913.7C707.9 915.1 706.5 917.5 705.2 919.0C703.9 920.6 702.6 923.0 702.2 924.5C701.6 927.4 696.6 938.6 694.2 942.7C693.4 944.1 692.0 946.8 691.2 948.7C690.3 950.6 688.8 954.0 687.7 956.2C686.7 958.4 685.2 962.2 684.3 964.7C683.5 967.1 681.9 970.0 680.9 971.2C679.0 973.2 673.7 985.7 672.4 991.2C672.0 992.8 670.6 995.3 669.3 996.7C668.0 998.1 666.4 1001.4 665.8 1004.2C665.2 1006.9 664.3 1009.6 663.8 1010.2C663.3 1010.9 662.0 1014.8 660.9 1019.0C659.9 1023.2 658.1 1027.9 657.0 1029.4C655.9 1031.0 654.3 1035.2 653.5 1038.8C652.6 1042.3 651.5 1046.5 650.8 1048.1C650.2 1049.8 649.3 1054.6 648.8 1058.8C648.2 1063.0 646.9 1069.0 645.9 1072.1C642.9 1080.8 641.6 1092.5 642.0 1106.9C642.4 1120.2 642.4 1120.2 646.3 1127.2C652.4 1138.1 655.4 1139.9 666.3 1139.1C671.2 1138.8 675.1 1137.7 679.5 1135.7ZM675.9 1096.8C674.5 1095.9 674.1 1089.6 675.4 1087.6C675.7 1087.1 676.6 1082.8 677.4 1078.1C678.1 1073.4 679.6 1068.1 680.7 1066.4C681.7 1064.6 683.2 1060.7 683.9 1057.7C684.6 1054.7 685.7 1050.7 686.5 1049.0C687.2 1047.2 688.3 1043.4 689.0 1040.5C689.6 1037.6 691.4 1032.8 693.0 1029.8C694.5 1026.8 695.8 1023.6 695.8 1022.7C695.8 1021.7 696.7 1019.0 697.8 1016.6C698.8 1014.1 700.2 1009.9 700.8 1007.2C701.4 1004.4 703.0 1000.6 704.2 998.7C705.5 996.8 707.1 993.4 707.8 991.2C708.5 989.0 709.6 986.3 710.3 985.1C711.1 984.0 712.6 979.8 713.8 975.9C714.9 972.0 716.3 968.5 716.8 968.2C717.3 967.9 718.4 965.3 719.4 962.4C720.3 959.5 721.9 956.0 723.0 954.5C724.0 953.1 724.8 951.3 724.8 950.6C724.8 948.8 732.8 931.6 734.9 928.9C735.9 927.6 737.4 924.3 738.4 921.6C740.7 914.6 744.1 908.8 748.3 904.7C751.2 901.9 752.2 901.4 753.6 902.3C756.3 904.0 756.2 909.8 753.5 918.9C752.1 923.5 750.7 929.2 750.3 931.7C749.9 934.2 748.6 938.0 747.3 940.3C746.0 942.6 744.6 946.4 744.2 948.8C743.8 951.2 742.7 955.1 741.7 957.5C740.6 959.9 739.3 965.1 738.7 969.0C738.1 973.0 736.6 978.0 735.3 980.3C734.0 982.5 732.4 986.6 731.8 989.3C731.1 992.0 729.9 995.2 729.2 996.4C728.4 997.7 727.3 1001.0 726.8 1003.7C726.3 1006.5 724.5 1011.9 722.8 1015.7C721.2 1019.4 719.8 1023.1 719.8 1023.9C719.8 1024.6 718.9 1027.0 717.7 1029.2C716.5 1031.3 715.0 1035.2 714.2 1037.8C713.4 1040.4 711.7 1044.5 710.3 1046.9C709.0 1049.2 707.8 1051.9 707.8 1052.8C707.8 1053.6 706.8 1055.3 705.5 1056.6C704.2 1057.8 702.6 1060.6 701.9 1062.9C701.2 1065.2 699.5 1068.7 698.2 1070.6C696.9 1072.6 695.8 1074.7 695.8 1075.3C695.8 1075.9 694.9 1077.2 693.7 1078.3C692.6 1079.3 690.6 1082.2 689.3 1084.7C683.8 1095.5 679.7 1099.2 675.9 1096.8ZM1257.0 632.6C1269.8 629.9 1273.8 625.1 1271.9 614.9C1271.0 610.1 1270.1 608.6 1266.6 605.4C1261.6 600.8 1250.1 595.1 1243.8 594.1C1241.4 593.7 1235.9 592.4 1231.7 591.1C1227.4 589.9 1219.7 588.3 1214.6 587.7C1209.4 587.1 1202.7 585.9 1199.7 585.1C1196.8 584.3 1189.4 583.4 1183.3 583.1C1172.5 582.5 1172.2 582.6 1168.1 585.3C1165.8 586.8 1163.1 589.6 1162.2 591.4C1160.4 594.6 1160.4 594.8 1162.8 599.1C1164.2 601.8 1167.3 605.0 1170.6 607.4C1173.6 609.5 1177.5 612.4 1179.3 613.9C1181.0 615.3 1183.8 616.9 1185.4 617.3C1187.0 617.7 1190.7 619.1 1193.6 620.4C1196.4 621.7 1200.3 623.0 1202.1 623.4C1203.9 623.7 1207.1 625.0 1209.4 626.3C1213.6 628.7 1219.8 630.0 1237.8 632.2C1251.9 633.9 1251.0 633.8 1257.0 632.6ZM212.6 527.3C209.8 525.9 209.1 520.8 211.1 516.9C215.3 509.0 218.2 504.1 219.9 501.9C221.0 500.5 221.8 499.0 221.8 498.4C221.8 497.5 224.1 493.7 229.7 484.9C231.0 482.9 234.1 477.4 236.6 472.7C239.0 468.0 241.8 463.3 242.8 462.2C243.7 461.1 245.5 458.2 246.8 455.7C248.0 453.2 249.9 449.4 251.0 447.2C252.2 445.0 253.7 442.5 254.5 441.6C256.3 439.5 262.8 428.7 262.8 427.7C262.8 427.3 264.2 425.2 265.9 423.1C267.6 420.9 270.0 417.5 271.2 415.3C273.4 411.3 277.0 405.3 280.3 399.7C281.4 397.9 283.3 395.2 284.6 393.7C285.8 392.1 286.8 390.5 286.8 390.0C286.8 389.6 288.5 386.7 290.6 383.6C292.6 380.5 295.5 376.1 296.8 373.8C301.8 365.5 305.8 359.1 307.8 356.2C308.9 354.5 311.0 351.6 312.3 349.7C317.0 343.0 321.8 336.1 326.8 329.2C329.6 325.3 334.5 318.3 337.8 313.5C341.1 308.8 344.1 304.7 344.6 304.5C345.5 304.2 348.9 299.4 353.3 292.2C354.4 290.5 356.0 288.3 356.8 287.5C357.7 286.6 359.9 283.8 361.8 281.3C368.1 272.9 393.1 242.3 401.6 232.5C403.9 229.9 406.5 226.3 407.3 224.7C408.2 223.0 409.3 221.7 409.9 221.7C410.5 221.7 412.6 219.8 414.6 217.5C424.8 205.9 467.9 162.3 481.5 150.0C492.7 139.8 503.4 131.4 511.7 126.2C512.6 125.6 515.0 123.8 517.0 122.2C523.3 117.1 526.2 115.0 529.5 113.3C531.3 112.4 533.2 110.8 533.8 109.7C534.4 108.6 535.5 107.7 536.3 107.7C538.0 107.7 552.5 100.3 554.7 98.3C557.1 96.2 567.3 91.8 575.1 89.7C578.9 88.6 583.4 86.9 585.2 85.7C588.1 84.0 590.2 83.7 600.0 83.7L611.4 83.7L617.3 88.5C623.0 93.1 623.5 93.9 625.6 101.0C628.2 110.0 628.4 116.0 626.3 128.2C625.4 133.1 624.3 140.1 623.7 143.7C623.1 147.3 621.5 152.5 620.2 155.3C618.9 158.1 617.8 161.0 617.8 161.7C617.8 162.4 616.8 164.9 615.4 167.1C614.1 169.4 612.7 173.0 612.3 175.2C612.0 177.4 609.7 182.8 607.3 187.2C604.9 191.6 601.9 197.2 600.7 199.7C599.5 202.2 597.8 205.3 596.8 206.7C595.9 208.1 594.8 210.3 594.3 211.7C593.9 213.1 592.6 215.3 591.3 216.7C590.1 218.1 587.4 222.3 585.3 226.0C583.3 229.8 580.8 233.6 579.8 234.5C577.9 236.4 570.8 247.3 570.8 248.5C570.8 248.9 569.0 251.5 566.7 254.2C562.9 258.8 551.2 273.9 546.3 280.6C545.2 282.1 540.5 287.6 535.9 292.8C531.3 298.0 525.2 304.9 522.4 308.1C519.6 311.4 515.8 315.7 513.8 317.8C511.9 319.8 508.8 323.4 506.9 325.8C501.2 332.9 470.2 364.1 458.8 374.1C446.8 384.7 436.1 393.6 429.3 398.7C426.7 400.6 421.5 404.9 417.8 408.2C410.7 414.5 399.8 423.1 393.8 427.2C391.9 428.5 388.6 430.9 386.4 432.4C382.6 435.2 379.0 437.6 370.7 443.1C368.3 444.7 366.2 446.4 366.0 446.9C365.8 447.4 363.5 448.9 361.0 450.3C352.9 454.6 348.9 457.3 346.8 459.5C345.7 460.7 344.2 461.7 343.4 461.7C342.7 461.7 340.4 463.0 338.3 464.7C336.3 466.3 334.0 467.7 333.2 467.7C332.5 467.7 330.4 469.0 328.6 470.6C326.8 472.1 321.5 475.3 316.7 477.6C312.0 479.8 307.3 482.6 306.3 483.7C305.3 484.8 304.0 485.7 303.3 485.7C302.6 485.7 300.4 486.9 298.2 488.3C296.1 489.8 293.0 491.4 291.4 492.0C288.5 493.0 281.8 496.8 274.8 501.4C272.8 502.6 270.6 503.7 269.8 503.7C269.0 503.7 265.2 505.3 261.4 507.2C257.6 509.1 253.9 510.7 253.3 510.7C252.7 510.7 251.3 511.6 250.3 512.7C249.3 513.8 246.2 515.4 243.4 516.2C240.6 517.0 237.0 518.6 235.3 519.7C233.7 520.8 231.6 521.7 230.7 521.7C229.7 521.7 227.6 522.9 225.8 524.3C222.1 527.5 215.9 528.9 212.6 527.3Z\"/></svg>";
  var IMG = 'assets/img/';

  /* ---------------------------------------------------------------- data */
  var STUDIO = {
    name: 'Relentless Frames',
    tagline: 'Photographs made by staying longer than anyone else would.',
    email: 'studio@relentlessframes.com',
    phone: '+1(573)953-2532',
    city: 'St. Louis, Missouri',
    instagram: 'relentlessframes'
  };

  // Five service categories. Each gets its own page and its own set of images
  // in assets/img/, named after the slug.
  var PROJECTS = [
    {
      slug: 'wedding',
      duration: '8–12 hours',
      title: 'Wedding',
      discipline: 'Wedding',
      place: 'St. Louis',
      blurb: 'The whole day, from the quiet of the morning to the last song.',
      story: [
        'A wedding is not a shot list. It is a long day with a handful of moments that matter enormously and cannot be repeated, scattered among hours that look like nothing until you see them printed.',
        'We shoot the whole arc — getting ready, the ceremony, the toasts nobody rehearsed, the floor at midnight. Available light wherever it will carry, flash only when the room leaves no choice.'
      ],
      facts: {
        'Typical coverage': '8–12 hours, two photographers',
        'Delivered': '600–900 edited frames',
        'Turnaround': 'Preview in one week, full gallery in six',
        'Also available': 'Rehearsal dinner, day-after session',
        'Travel': 'Missouri and Illinois included'
      }
    },
    {
      slug: 'portraits',
      heroFocus: 22, thumbFocus: 35,
      duration: '60–90 min',
      title: 'Portraits',
      discipline: 'Portraits',
      place: 'St. Louis',
      blurb: 'One person, one light, and long enough for the posing to wear off.',
      story: [
        'Headshots, senior sessions, families, personal branding. The technical part is quick. The part that takes time is waiting out the face people put on for cameras.',
        'Sessions run an hour minimum for that reason. Studio, your home, or somewhere in the city that means something to you — the location matters less than the time.'
      ],
      facts: {
        'Session length': '60–90 minutes',
        'Delivered': '25–40 edited frames',
        'Turnaround': 'Ten working days',
        'Includes': 'Wardrobe guidance ahead of the session',
        'Studio or location': 'Either, same rate'
      }
    },
    {
      slug: 'events',
      duration: '3–8 hours',
      title: 'Events',
      discipline: 'Events',
      place: 'St. Louis',
      blurb: 'Conferences, galas and launches, covered without interrupting them.',
      story: [
        'Corporate and private events, photographed the way a documentary photographer would: quietly, from inside the room, without staging a single handshake.',
        'You get the keynote and the step-and-repeat because you need them. You also get the hallway conversations, which is usually where the pictures people actually use come from.'
      ],
      facts: {
        'Typical coverage': '3–8 hours',
        'Delivered': '200–500 edited frames',
        'Turnaround': 'Same-night selects, full gallery in two weeks',
        'Also available': 'On-site printing, live gallery feed',
        'Licensing': 'Full commercial use included'
      }
    },
    {
      slug: 'church-worship',
      heroFocus: 16, thumbFocus: 16,
      duration: '2–4 hours',
      title: 'Church & Worship',
      discipline: 'Church & Worship',
      place: 'St. Louis',
      blurb: 'Services, baptisms and ministry work, photographed with the room respected.',
      story: [
        'Worship spaces are hard to photograph well: mixed lighting, deep balconies, and a room where a shutter carries. We work silent, on long glass, from the back.',
        'Regular Sunday coverage, baptisms, dedications, choir and ministry portraits, and image libraries for church websites and print. We have worked with congregations from forty people to two thousand.'
      ],
      facts: {
        'Typical coverage': '2–4 hours',
        'Delivered': '120–300 edited frames',
        'Turnaround': 'One week',
        'Working method': 'Silent shutter, no flash during service',
        'Also available': 'Ongoing monthly retainer'
      }
    },
    {
      slug: 'lifestyle-candid',
      heroFocus: 26, thumbFocus: 26,
      duration: '2 hours',
      title: 'Lifestyle & Candid',
      discipline: 'Lifestyle & Candid',
      place: 'St. Louis',
      blurb: 'Unposed, in daylight, in the places people actually spend their time.',
      story: [
        'Families at home, small businesses at work, couples doing nothing in particular. No posing beyond putting people somewhere with decent light and letting them get on with it.',
        'These sessions are slower on purpose. The first twenty minutes are usually unusable and everyone knows it. What comes after is the reason to book them.'
      ],
      facts: {
        'Session length': '2 hours',
        'Delivered': '60–100 edited frames',
        'Turnaround': 'Ten working days',
        'Best time': 'The two hours before sunset'
      }
    }
  ];

  var PROCESS = [
    ['01', 'Conversation', 'Before anything is quoted we talk about what the pictures are actually for — a gallery wall, a church website, a company profile, an album your family will still open in thirty years. The end use decides everything upstream of it.'],
    ['02', 'The plan', 'For anything location-based we visit first, at the hour we intend to shoot. A sanctuary at 10 a.m. and the same sanctuary at 4 p.m. are two different rooms, and we would rather find that out before the day.'],
    ['03', 'The shoot', 'We work long and we work quietly. No shot list is treated as complete, and we stay past the point where the schedule says we are finished — which is usually where the frames are.'],
    ['04', 'Edit & hand-off', 'A tight selection, graded consistently, delivered in web and print resolutions with a licence written in plain English. Full unedited takes are archived for five years.']
  ];

  // Individual shoots. `slug` matches an entry in assets/data/images.json
  // (falls back to filenames in assets/img/ if the manifest is missing).
  // `cat` points at a PROJECTS slug.
  var SHOOTS = [
    // ---- Wedding ------------------------------------------------------
    {
      cat: 'wedding', slug: 'charis-emma', title: 'Charis & Emma',
      plateCount: 3,
      platesFocus: [null, 25, 40],
      when: 'Recent',
      blurb: 'A full day of celebration, from quiet morning moments to the last dance.',
      story: [
        'Covered the way every wedding here is — quietly, in available light, staying through to the moments most coverage misses.'
      ],
      facts: { Note: 'Full gallery available on request' }
    },
    {
      cat: 'wedding', slug: 'praise-bolu', title: 'Praise & Bolu',
      plateCount: 6,
      heroFocus: 11, thumbFocus: 11,
      platesFocus: [null, 39, 36, null, 40, null],
      when: 'Recent',
      blurb: 'Two families, one day, and a celebration carried through every hour of it.',
      story: [
        'Covered the way every wedding here is — quietly, in available light, staying through to the moments most coverage misses.'
      ],
      facts: { Note: 'Full gallery available on request' }
    },
    {
      cat: 'wedding', slug: 'taylor-ade', title: 'Taylor & Ade',
      plateCount: 10,
      heroFocus: 28, thumbFocus: 28,
      platesFocus: [43, null, 26, null, 24, 11, 39, 9, null, null],
      when: 'Recent',
      blurb: 'A wedding day worked in full, from the getting-ready hours through the reception.',
      story: [
        'Covered the way every wedding here is — quietly, in available light, staying through to the moments most coverage misses.'
      ],
      facts: { Note: 'Full gallery available on request' }
    },
    {
      cat: 'wedding', slug: 'tomi-sam', title: 'Tomi & Sam',
      plateCount: 4,
      heroFocus: 38, thumbFocus: 38,
      platesFocus: [26, 26, 32, 46],
      when: 'Recent',
      blurb: 'A day built around the people in it, photographed as it actually happened.',
      story: [
        'Covered the way every wedding here is — quietly, in available light, staying through to the moments most coverage misses.'
      ],
      facts: { Note: 'Full gallery available on request' }
    },

    // ---- Events -------------------------------------------------------
    {
      cat: 'events', slug: 'igala-day', title: "Igala Day '26",
      plateCount: 7,
      platesFocus: [35, 34, 25, 35, 28, 37, 27],
      when: 'Recent',
      blurb: "A cultural celebration, covered from the opening procession through the night's close.",
      story: [
        'Documented as it unfolded — the ceremony, the dancing, the details that made the day.'
      ],
      facts: { Note: 'Full gallery available on request' }
    },
    {
      cat: 'events', slug: 'wedding-party', title: 'Wedding Party',
      plateCount: 4,
      heroFocus: 41, thumbFocus: 41,
      platesFocus: [null, 39, 23, 38],
      when: 'Recent',
      blurb: 'The reception in full swing, photographed as a celebration rather than a formality.',
      story: [
        'Covered quietly from inside the room, the way any celebration deserves to be documented.'
      ],
      facts: { Note: 'Full gallery available on request' }
    },
    {
      cat: 'events', slug: 'worship-concert', title: 'Worship Concert',
      plateCount: 8,
      heroFocus: 26, thumbFocus: 26,
      platesFocus: [15, null, 15, 24, 11, null, 29, 34],
      when: 'Recent',
      blurb: 'A night of worship, photographed from the crowd and the stage alike.',
      story: [
        'Low light, a moving crowd, and a set covered without a single staged frame.'
      ],
      facts: { Note: 'Full gallery available on request' }
    },

    // ---- Church & Worship ---------------------------------------------
    {
      cat: 'church-worship', slug: 'sunday-worship', title: 'Sunday Worship',
      plateCount: 11,
      heroFocus: 16, thumbFocus: 16,
      platesFocus: [31, 42, 14, null, 29, 31, 36, null, null, null, 40],
      when: 'Recent',
      blurb: 'Services and ministry moments from across the year, photographed quietly from the back of the room.',
      story: [
        'A running collection of Sunday coverage — worship, baptisms, and the everyday life of the congregation.'
      ],
      facts: { Note: 'Full gallery available on request' }
    },

    // ---- Lifestyle & Candid --------------------------------------------
    {
      cat: 'lifestyle-candid', slug: 'lifestyle-sessions', title: 'Lifestyle Sessions',
      plateCount: 12,
      heroFocus: 26, thumbFocus: 26,
      platesFocus: [42, 28, 22, 16, null, null, null, 29, null, 42, 33, 29],
      when: 'Recent',
      blurb: 'Unposed moments from everyday life, photographed as they happened.',
      story: [
        'A collection of candid sessions — families, routines, and ordinary afternoons, photographed without posing.'
      ],
      facts: { Note: 'Full gallery available on request' }
    },

    // ---- Portraits ------------------------------------------------------
    {
      cat: 'portraits', slug: 'portrait-sessions', title: 'Portrait Sessions',
      plateCount: 6,
      heroFocus: 22, thumbFocus: 35,
      platesFocus: [29, 32, 19, 33, 23, 22],
      when: 'Recent',
      blurb: 'Individual sessions from across the year, photographed with one light and no rush.',
      story: [
        'A running collection of portrait sittings — headshots, personal sessions, and the occasional favor for a friend.'
      ],
      facts: { Note: 'Full gallery available on request' }
    }
  ];

  var shootsIn = function (catSlug) {
    return SHOOTS.filter(function (s) { return s.cat === catSlug; });
  };
  var shootBySlug = function (slug) {
    for (var i = 0; i < SHOOTS.length; i++) if (SHOOTS[i].slug === slug) return SHOOTS[i];
    return null;
  };

  /* ------------------------------------------------------------- helpers */
  var $ = function (s, r) { return (r || document).querySelector(s); };
  var $$ = function (s, r) { return Array.prototype.slice.call((r || document).querySelectorAll(s)); };
  var esc = function (s) {
    return String(s).replace(/[&<>"']/g, function (c) {
      return { '&': '&amp;', '<': '&lt;', '>': '&gt;', '"': '&quot;', "'": '&#39;' }[c];
    });
  };
  var bySlug = function (s) {
    for (var i = 0; i < PROJECTS.length; i++) if (PROJECTS[i].slug === s) return PROJECTS[i];
    return null;
  };
  var galleryOf = function (p) {
    return ['01', '02', '03', '04'].map(function (n) { return IMG + p.slug + '-' + n + '.jpg'; });
  };

  /* ------------------------------------------------------ image manifest
     assets/data/images.json lets the studio owner swap photos from the
     admin CMS without touching code. Every lookup below falls back to the
     original filename convention, so the site renders identically even if
     the manifest is missing, fails to load, or is opened via file://. */
  var IMAGES = { studio: null, categories: {}, shoots: {} };
  function loadImages() {
    return fetch('assets/data/images.json', { cache: 'no-cache' })
      .then(function (r) { return r.ok ? r.json() : null; })
      .then(function (data) {
        if (!data) return;
        IMAGES.studio = data.studio || null;
        (data.categories || []).forEach(function (c) {
          if (c && c.slug) IMAGES.categories[c.slug] = c;
        });
        (data.shoots || []).forEach(function (s) {
          if (s && s.slug) IMAGES.shoots[s.slug] = s;
        });
      })
      .catch(function () { /* offline / file:// / no manifest yet — use defaults */ });
  }
  function catHero(p) {
    var m = IMAGES.categories[p.slug];
    return (m && m.hero) || (IMG + p.slug + '-hero.jpg');
  }
  function catThumb(p) {
    var m = IMAGES.categories[p.slug];
    return (m && m.thumb) || (IMG + p.slug + '-thumb.jpg');
  }
  function shootHero(sh) {
    var m = IMAGES.shoots[sh.slug];
    return (m && m.hero) || (IMG + sh.slug + '-hero.jpg');
  }
  function shootThumb(sh) {
    var m = IMAGES.shoots[sh.slug];
    return (m && m.thumb) || (IMG + sh.slug + '-thumb.jpg');
  }
  function shootPlates(sh) {
    var m = IMAGES.shoots[sh.slug];
    if (m && Array.isArray(m.plates) && m.plates.length) return m.plates;
    if (m) {
      // Back-compat with the older fixed plate1..plate4 manifest shape.
      var fixed = ['plate1', 'plate2', 'plate3', 'plate4']
        .map(function (key) { return m[key]; })
        .filter(Boolean);
      if (fixed.length) return fixed;
    }
    // Manifest unreachable (e.g. opened via file://, where fetch() of a
    // local JSON file is blocked) — fall back to the naming convention,
    // using each shoot's own photo count rather than assuming four, since
    // real shoots here range from 3 to 12 gallery photos.
    var count = sh.plateCount || 4;
    var out = [];
    for (var i = 1; i <= count; i++) {
      out.push(IMG + sh.slug + '-' + (i < 10 ? '0' : '') + i + '.jpg');
    }
    return out;
  }
  function studioImg(n) {
    var m = IMAGES.studio;
    var key = 'photo' + n;
    // studio-01/02.jpg were blank placeholder art. Until real behind-the-
    // scenes studio photos are supplied, these two faceless detail shots
    // (a ring exchange, a reception table) stand in as fallback so the
    // About page never shows an empty grey block.
    var fallback = n === 1 ? 'taylor-ade-09.jpg' : 'taylor-ade-04.jpg';
    return (m && m[key]) || (IMG + fallback);
  }

  /* Some source photos put the subject's face well above the vertical
     centre (a full-length shot, a bride under a veil, a speaker at a
     lectern). Every hero/thumb/plate frame crops with object-fit:cover,
     which centres on the image by default — fine for a landscape shot,
     but it can crop straight through someone's face when the frame is
     much shorter than the source photo. Where a *Focus value (0-100, the
     vertical % to centre on) is available we use it instead of the CSS
     default of 50%.

     These values live in two places and are merged, manifest first:
     assets/data/images.json (editable from the CMS, so a photo swapped
     in later can carry its own focus point) falling back to the
     hard-coded PROJECTS/SHOOTS data below (so the fix still applies when
     the manifest can't be fetched at all — notably over file://, where
     browsers block fetch() of local JSON and the CMS is moot anyway
     since there's no server to save back to). */
  function firstNum() {
    for (var i = 0; i < arguments.length; i++) {
      if (typeof arguments[i] === 'number') return arguments[i];
    }
    return undefined;
  }
  function posStyle(v) {
    return (typeof v === 'number') ? ' style="object-position:50% ' + v + '%"' : '';
  }
  function catHeroFocus(p) {
    var m = IMAGES.categories[p.slug];
    return firstNum(m && m.heroFocus, p.heroFocus);
  }
  function catThumbFocus(p) {
    var m = IMAGES.categories[p.slug];
    return firstNum(m && m.thumbFocus, p.thumbFocus);
  }
  function shootHeroFocus(sh) {
    var m = IMAGES.shoots[sh.slug];
    return firstNum(m && m.heroFocus, sh.heroFocus);
  }
  function shootThumbFocus(sh) {
    var m = IMAGES.shoots[sh.slug];
    return firstNum(m && m.thumbFocus, sh.thumbFocus);
  }
  function shootPlateFocus(sh, i) {
    var m = IMAGES.shoots[sh.slug];
    var fromManifest = m && Array.isArray(m.platesFocus) ? m.platesFocus[i] : undefined;
    var fromStatic = Array.isArray(sh.platesFocus) ? sh.platesFocus[i] : undefined;
    return firstNum(fromManifest, fromStatic);
  }

  /* ---------------------------------------------------------------- chrome */
  function markSvg() { return MARK; }

  function buildChrome() {
    var nav = document.createElement('nav');
    nav.className = 'nav';
    nav.innerHTML =
      '<button class="menu-btn" id="menuBtn" aria-label="Open menu" aria-expanded="false">' +
        '<span class="burger" aria-hidden="true"><span></span><span></span><span></span></span>' +
        '<span class="label">Menu</span>' +
      '</button>' +
      '<a class="wordmark" href="#/" aria-label="Relentless Frames — home">' +
        '<span class="mark" aria-hidden="true">' + markSvg() + '</span>' +
        '<span class="type"><i>Relentless</i><i>Frames</i></span>' +
      '</a>' +
      '<button class="search-btn" id="searchBtn" aria-label="Search">' +
        '<svg viewBox="0 0 20 20" aria-hidden="true"><circle cx="9" cy="9" r="6"/><line x1="13.5" y1="13.5" x2="18" y2="18"/></svg>' +
      '</button>';

    var drawer = document.createElement('div');
    drawer.className = 'drawer';
    drawer.id = 'drawer';
    drawer.setAttribute('role', 'dialog');
    drawer.setAttribute('aria-label', 'Main menu');
    drawer.innerHTML =
      '<div class="wrap"><div class="drawer-links">' +
        [['Work', '#/work'], ['Studio', '#/studio'], ['Process', '#/process'], ['Contact', '#/contact']]
          .map(function (l) {
            return '<a href="' + l[1] + '" data-route="' + l[1] +
                   '"><span class="lbl">' + l[0] + '</span></a>';
          }).join('') +
      '</div></div>' +
      '<div class="drawer-foot">' +
        '<span class="eyebrow">' + esc(STUDIO.city) + '</span>' +
        '<a class="eyebrow" href="mailto:' + esc(STUDIO.email) + '">' + esc(STUDIO.email) + '</a>' +
      '</div>';

    var search = document.createElement('div');
    search.className = 'search';
    search.id = 'search';
    search.setAttribute('role', 'dialog');
    search.setAttribute('aria-label', 'Search');
    search.innerHTML =
      '<div class="search-inner">' +
        '<input id="searchInput" type="search" placeholder="Search projects" autocomplete="off" spellcheck="false">' +
        '<div class="search-results" id="searchResults"></div>' +
      '</div>';

    var app = document.createElement('main');
    app.id = 'app';

    document.body.appendChild(nav);
    document.body.appendChild(drawer);
    document.body.appendChild(search);
    document.body.appendChild(app);
  }

  /* ------------------------------------------------------------- overlays */
  var openPanel = null;

  function setPanel(which) {
    var drawer = $('#drawer'), search = $('#search'), menuBtn = $('#menuBtn');
    openPanel = which;
    drawer.classList.toggle('is-open', which === 'menu');
    search.classList.toggle('is-open', which === 'search');
    menuBtn.classList.toggle('is-open', which === 'menu');
    menuBtn.setAttribute('aria-expanded', which === 'menu' ? 'true' : 'false');
    document.body.classList.toggle('is-locked', !!which);
    if (which === 'search') {
      renderSearch('');
      setTimeout(function () { $('#searchInput').focus(); }, 60);
    }
    syncNavTone();
  }

  function renderSearch(q) {
    var box = $('#searchResults');
    var term = q.trim().toLowerCase();
    var pool = PROJECTS.map(function (p) {
      return { href: '#/work/' + p.slug, title: p.title, sub: p.place,
               hay: p.title + ' ' + p.discipline + ' ' + p.place + ' ' + p.blurb };
    }).concat(SHOOTS.map(function (sh) {
      var c = bySlug(sh.cat);
      return { href: '#/work/' + sh.cat + '/' + sh.slug, title: sh.title,
               sub: (c ? c.title : '') + ' · ' + sh.when,
               hay: sh.title + ' ' + sh.when + ' ' + sh.blurb + ' ' + (c ? c.title : '') };
    }));
    var hits = pool.filter(function (x) {
      return !term || x.hay.toLowerCase().indexOf(term) > -1;
    });
    if (!hits.length) {
      box.innerHTML = '<p class="search-empty">No projects match “' + esc(q) + '”.</p>';
      return;
    }
    box.innerHTML = hits.map(function (x) {
      return '<a href="' + x.href + '" data-close>' +
        '<span class="t">' + esc(x.title) + '</span>' +
        '<span class="eyebrow">' + esc(x.sub) + '</span>' +
        '</a>';
    }).join('');
  }

  /* ----------------------------------------------------------------- views */
  function viewHome() {
    return '' +
      '<section class="home">' +
        '<div class="home-stage">' +
          PROJECTS.map(function (p, i) {
            // Every hero loads eagerly. They sit in the viewport at zero
            // opacity, so a lazy one is not fetched until it is needed — and
            // then the first swipe crossfades to an image that has not
            // arrived yet, which reads as the swipe doing nothing.
            return '<div class="home-slide' + (i === 0 ? ' is-active' : '') + '" data-i="' + i + '">' +
              '<img src="' + catHero(p) + '" alt="" draggable="false" ' +
              'decoding="async" fetchpriority="' + (i === 0 ? 'high' : 'low') + '"' +
              posStyle(catHeroFocus(p)) + '>' +
            '</div>';
          }).join('') +
        '</div>' +
        '<div class="home-caption in" id="homeCaption">' +
          '<h1 class="t">' + esc(PROJECTS[0].title) + '</h1>' +
          '<p class="s">' + esc(PROJECTS[0].blurb) + '</p>' +
        '</div>' +
        '<div class="strip">' +
          '<div class="strip-viewport"><div class="strip-track" id="stripTrack"></div></div>' +
        '</div>' +
        '<div class="baseline"><i id="stripBar"></i></div>' +
        '<div class="strip-count"><span id="stripNum">01</span> / ' +
          (PROJECTS.length < 10 ? '0' : '') + PROJECTS.length + '</div>' +
      '</section>';
  }

  function shell(inner, opts) {
    return '<div class="page"><div class="page-main">' + inner + '</div>' + footer(opts) + '</div>';
  }

  /* Footer, after the reference: a CTA block with the mark, labelled link
     columns, then a fine-print row. Shown on every inner page — except the
     CTA block itself, which is skipped on the Contact page since a "tell us
     about your shoot" prompt makes no sense when you're already there. */
  function footer(opts) {
    opts = opts || {};
    var cats = PROJECTS.map(function (p) {
      return '<a href="#/work/' + p.slug + '">' + esc(p.title) + '</a>';
    }).join('');

    return '' +
      '<footer class="foot"><div class="wrap">' +

        (opts.hideCta ? '' :
        '<div class="foot-cta">' +
          '<div class="foot-cta-text">' +
            '<span class="kicker">Start a project</span>' +
            '<a class="cta-link" href="#/contact">Tell us about your shoot</a>' +
          '</div>' +
          '<span class="cta-mark" aria-hidden="true">' + markSvg() + '</span>' +
        '</div>') +

        '<div class="foot-cols">' +
          '<nav class="foot-col">' +
            '<span class="kicker">Index</span>' +
            '<a href="#/work">Work</a><a href="#/studio">Studio</a>' +
            '<a href="#/process">Process</a><a href="#/contact">Contact</a>' +
          '</nav>' +
          '<nav class="foot-col">' +
            '<span class="kicker">Categories</span>' + cats +
          '</nav>' +
          '<div class="foot-col">' +
            '<span class="kicker">Studio</span>' +
            '<address>' + esc(STUDIO.city) + '<br>By appointment</address>' +
          '</div>' +
          '<div class="foot-col">' +
            '<span class="kicker">Connect</span>' +
            '<a href="mailto:' + esc(STUDIO.email) + '">' + esc(STUDIO.email) + '</a>' +
            '<a href="tel:' + esc(STUDIO.phone.replace(/[^0-9+]/g, '')) + '">' + esc(STUDIO.phone) + '</a>' +
            '<a href="#/contact">Instagram</a>' +
          '</div>' +
        '</div>' +

        '<div class="foot-bot">' +
          '<span>© ' + new Date().getFullYear() + ' Relentless Frames</span>' +
          '<span>Weddings · Portraits · Events · Worship · Lifestyle</span>' +
          '<span>' + esc(STUDIO.city) + '</span>' +
        '</div>' +

      '</div></footer>';
  }

  /* Work index: every shoot, flat, after the reference's Work page. Unlike
     the reference's All/Commercial/Residential tags (which just filter —
     there's no separate "Residential" page to visit), each RF category is
     a real destination with its own story and pricing facts, so the chips
     here are links: "All" stays on this grid, each category jumps to its
     page. That page's own shoot-list is still the way to browse just that
     category without leaving it. */
  function viewWork() {
    return shell(
      '<div class="wrap">' +
        '<header class="page-head">' +
          '<span class="eyebrow">' + SHOOTS.length + ' shoots · ' + esc(STUDIO.city) + '</span>' +
        '</header>' +
        '<h1 class="page-title">Work</h1>' +

        '<nav class="filters" aria-label="Categories">' +
          '<a href="#/work" aria-current="page">All <span class="count">' + SHOOTS.length + '</span></a>' +
          PROJECTS.map(function (p) {
            return '<a href="#/work/' + p.slug + '">' + esc(p.title) +
              ' <span class="count">' + shootsIn(p.slug).length + '</span></a>';
          }).join('') +
        '</nav>' +

        '<div class="grid" id="grid">' +
          SHOOTS.map(function (sh) {
            var cat = bySlug(sh.cat);
            return '<a class="card reveal" href="#/work/' + sh.cat + '/' + sh.slug + '">' +
              '<div class="card-frame"><img src="' + shootThumb(sh) + '" alt="' + esc(sh.title) + '" loading="lazy"' +
              posStyle(shootThumbFocus(sh)) + '></div>' +
              '<div class="card-meta">' +
                '<span class="t">' + esc(sh.title) + '</span>' +
                '<span class="m">' + esc(cat ? cat.title : '') + ' · ' + esc(sh.when) + '</span>' +
              '</div>' +
            '</a>';
          }).join('') +
        '</div>' +
      '</div>'
    );
  }

  /* Category page: the rail carries the offer, the body indexes the shoots. */
  function viewProject(slug) {
    var p = bySlug(slug);
    if (!p) return viewMissing();
    var idx = PROJECTS.indexOf(p);
    var next = PROJECTS[(idx + 1) % PROJECTS.length];
    var list = shootsIn(p.slug);

    return shell(
      '<div class="proj-hero"><img src="' + catHero(p) + '" alt="' +
        esc(p.title) + '" draggable="false"' + posStyle(catHeroFocus(p)) + '></div>' +

      '<div class="wrap">' +
        '<div class="detail">' +

          '<aside class="rail">' +
            '<div class="rail-inner">' +
              '<span class="rail-mark" aria-hidden="true">' + markSvg() + '</span>' +
              '<h1 class="rail-title">' + esc(p.title) + '</h1>' +
              '<span class="kicker rail-kicker">' + esc(p.place) + '</span>' +
              '<dl class="rail-meta">' +
                Object.keys(p.facts).map(function (k) {
                  return '<div><dt>' + esc(k) + '</dt><dd>' + esc(p.facts[k]) + '</dd></div>';
                }).join('') +
              '</dl>' +
              '<a class="rail-back" href="#/work">← All categories</a>' +
            '</div>' +
          '</aside>' +

          '<div class="detail-body">' +
            '<div class="overview prose">' +
              p.story.map(function (s) { return '<p>' + esc(s) + '</p>'; }).join('') +
            '</div>' +

            '<div class="shoot-head">' +
              '<span class="kicker">Selected work</span>' +
              '<span class="kicker">' + list.length + ' ' +
                (list.length === 1 ? 'shoot' : 'shoots') + '</span>' +
            '</div>' +

            '<div class="shoot-list">' +
              list.map(function (sh) {
                return '<a class="shoot-card reveal" href="#/work/' + p.slug + '/' + sh.slug + '">' +
                  '<div class="shoot-frame">' +
                    '<img src="' + shootThumb(sh) + '" alt="' + esc(sh.title) +
                    '" loading="lazy" draggable="false"' + posStyle(shootThumbFocus(sh)) + '>' +
                  '</div>' +
                  '<div class="shoot-info">' +
                    '<h3 class="shoot-title">' + esc(sh.title) + '</h3>' +
                    '<p class="shoot-blurb">' + esc(sh.blurb) + '</p>' +
                    '<span class="shoot-meta">' + esc(sh.when) + '</span>' +
                  '</div>' +
                '</a>';
              }).join('') +
            '</div>' +

            '<a class="next-proj" href="#/work/' + next.slug + '">' +
              '<span class="kicker">Next category</span>' +
              '<span class="n">' + esc(next.title) + '</span>' +
            '</a>' +
          '</div>' +

        '</div>' +
      '</div>'
    );
  }

  /* Shoot page: one job, told properly, with its plates. */
  function viewShoot(catSlug, slug) {
    var sh = shootBySlug(slug);
    var cat = bySlug(catSlug);
    if (!sh || !cat || sh.cat !== catSlug) return viewMissing();

    var siblings = shootsIn(catSlug);
    var i = siblings.indexOf(sh);
    var next = siblings[(i + 1) % siblings.length];
    var plates = shootPlates(sh);

    return shell(
      '<div class="proj-hero"><img src="' + shootHero(sh) + '" alt="' +
        esc(sh.title) + '" draggable="false"' + posStyle(shootHeroFocus(sh)) + '></div>' +

      '<div class="wrap">' +
        '<nav class="crumbs" aria-label="Breadcrumb">' +
          '<a href="#/work">Work</a><span>/</span>' +
          '<a href="#/work/' + cat.slug + '">' + esc(cat.title) + '</a><span>/</span>' +
          '<span class="here">' + esc(sh.title) + '</span>' +
        '</nav>' +

        '<div class="detail">' +

          '<aside class="rail">' +
            '<div class="rail-inner">' +
              '<span class="rail-mark" aria-hidden="true">' + markSvg() + '</span>' +
              '<h1 class="rail-title">' + esc(sh.title) + '</h1>' +
              '<span class="kicker rail-kicker">' + esc(sh.when) + '</span>' +
              '<dl class="rail-meta">' +
                Object.keys(sh.facts).map(function (k) {
                  return '<div><dt>' + esc(k) + '</dt><dd>' + esc(sh.facts[k]) + '</dd></div>';
                }).join('') +
              '</dl>' +
              '<a class="rail-back" href="#/work/' + cat.slug + '">← All ' +
                esc(cat.title.toLowerCase()) + '</a>' +
            '</div>' +
          '</aside>' +

          '<div class="detail-body">' +
            '<div class="overview prose">' +
              '<p class="shoot-lede">' + esc(sh.blurb) + '</p>' +
              sh.story.map(function (s) { return '<p>' + esc(s) + '</p>'; }).join('') +
            '</div>' +

            '<div class="gallery reveal" id="gallery">' +
              '<div class="gallery-stage">' +
                '<button class="gallery-arrow prev" type="button" aria-label="Previous image">' +
                  '<svg viewBox="0 0 20 20" aria-hidden="true"><path d="M12.5 4l-6 6 6 6" fill="none" stroke="currentColor" stroke-width="1.4" stroke-linecap="round" stroke-linejoin="round"/></svg>' +
                '</button>' +
                '<div class="gallery-frame">' +
                  plates.map(function (src, k) {
                    return '<div class="gallery-slide' + (k === 0 ? ' is-active' : '') + '" data-i="' + k + '">' +
                      '<img src="' + src + '" alt="" loading="' + (k === 0 ? 'eager' : 'lazy') + '" draggable="false"' +
                      posStyle(shootPlateFocus(sh, k)) + '>' +
                    '</div>';
                  }).join('') +
                '</div>' +
                '<button class="gallery-arrow next" type="button" aria-label="Next image">' +
                  '<svg viewBox="0 0 20 20" aria-hidden="true"><path d="M7.5 4l6 6-6 6" fill="none" stroke="currentColor" stroke-width="1.4" stroke-linecap="round" stroke-linejoin="round"/></svg>' +
                '</button>' +
                '<span class="gallery-count"><span id="galleryNum">01</span> / ' +
                  (plates.length < 10 ? '0' : '') + plates.length + '</span>' +
              '</div>' +
              '<div class="gallery-thumbs">' +
                plates.map(function (src, k) {
                  return '<button class="gallery-thumb' + (k === 0 ? ' is-active' : '') + '" type="button" data-i="' + k + '" aria-label="Image ' + (k + 1) + ' of ' + plates.length + '">' +
                    '<img src="' + src + '" alt="" loading="lazy"' + posStyle(shootPlateFocus(sh, k)) + '>' +
                  '</button>';
                }).join('') +
              '</div>' +
            '</div>' +

            '<a class="next-proj" href="#/work/' + catSlug + '/' + next.slug + '">' +
              '<span class="kicker">Next in ' + esc(cat.title.toLowerCase()) + '</span>' +
              '<span class="n">' + esc(next.title) + '</span>' +
            '</a>' +
          '</div>' +

        '</div>' +
      '</div>'
    );
  }

  function viewStudio() {
    return shell(
      '<div class="wrap">' +
        '<header class="page-head"><span class="eyebrow">Studio</span></header>' +
        '<h1 class="page-title" style="margin-bottom:44px">Relentless<br>Frames</h1>' +
        '<div class="two-col">' +
          '<div class="a"><p class="lede">' + esc(STUDIO.tagline) + '</p></div>' +
          '<div class="b prose">' +
            '<p>We are a two-person photography studio in St. Louis, working across weddings, portraits, events, worship and lifestyle. The categories look unrelated on paper. In practice they are the same job: turn up early, understand the light, and refuse to leave before the picture exists.</p>' +
            '<p>Most of what we are hired for could be photographed adequately in two hours. We do not work that way. The frames clients keep — the ones that end up printed, framed, or on a cover — almost always arrive after the point where a faster photographer would have packed up.</p>' +
            '<p>We shoot digital and film, grade everything by hand, and keep the retouching light enough that people still look like themselves.</p>' +
          '</div>' +
        '</div>' +
        '<div class="img-duo">' +
          '<figure class="reveal" style="margin:0"><img src="' + studioImg(1) + '" alt="" loading="lazy"></figure>' +
          '<figure class="reveal" style="margin:0"><img src="' + studioImg(2) + '" alt="" loading="lazy"></figure>' +
        '</div>' +
        '<div class="two-col">' +
          '<div class="a"><span class="eyebrow">Recognition &amp; clients</span></div>' +
          '<div class="b prose">' +
            '<p>We photograph for congregations, companies and a long list of families who would rather not be named here. Personal work has shown twice in St. Louis.</p>' +
            '<p>Available across Missouri and Illinois at no travel charge, and further out by arrangement.</p>' +
          '</div>' +
        '</div>' +
      '</div>'
    );
  }

  function viewProcess() {
    return shell(
      '<div class="wrap">' +
        '<header class="page-head"><span class="eyebrow">How we work</span></header>' +
        '<h1 class="page-title" style="margin-bottom:34px">Process</h1>' +
        '<div class="two-col" style="padding-bottom:34px">' +
          '<div class="a"><p class="lede">Four stages, and none of them is rushed.</p></div>' +
          '<div class="b prose"><p>Every commission runs the same way whether it is a one-hour product set or a two-day wedding. The scale changes; the sequence does not.</p></div>' +
        '</div>' +
        '<div class="steps">' +
          PROCESS.map(function (s) {
            return '<div class="step reveal">' +
              '<span class="n">' + s[0] + '</span>' +
              '<h3>' + esc(s[1]) + '</h3>' +
              '<p class="d" style="margin:0">' + esc(s[2]) + '</p>' +
            '</div>';
          }).join('') +
        '</div>' +
      '</div>'
    );
  }

  function viewContact() {
    return shell(
      '<div class="wrap">' +
        '<header class="page-head"><span class="eyebrow">Enquiries</span></header>' +
        '<h1 class="page-title" style="margin-bottom:48px">Contact</h1>' +
        '<div class="contact-grid">' +
          '<div class="a">' +
            '<a class="big-link" href="mailto:' + esc(STUDIO.email) + '">' + esc(STUDIO.email) + '</a>' +
            '<dl class="detail-list">' +
              '<div><dt>Studio</dt><dd>' + esc(STUDIO.city) + '<br>By appointment</dd></div>' +
              '<div><dt>Telephone</dt><dd>' + esc(STUDIO.phone) + '</dd></div>' +
              '<div><dt>Instagram</dt><dd>@' + esc(STUDIO.instagram) + '</dd></div>' +
              '<div><dt>Response time</dt><dd>Within two working days. Wedding dates are held for seven days once a deposit invoice is issued.</dd></div>' +
            '</dl>' +
          '</div>' +
          '<div class="b">' +
            '<form class="form" id="enquiry">' +
              '<div class="field-row">' +
                '<div class="field"><label for="f-name">Name</label><input id="f-name" name="name" required></div>' +
                '<div class="field"><label for="f-email">Email</label><input id="f-email" name="email" type="email" required></div>' +
              '</div>' +
              '<div class="field-row">' +
                '<div class="field"><label for="f-type">Commission type</label>' +
                  '<select id="f-type" name="type">' +
                    '<option>Editorial</option><option>Wedding</option>' +
                    '<option>Architecture / interiors</option><option>Commercial / product</option>' +
                    '<option>Something else</option>' +
                  '</select></div>' +
                '<div class="field"><label for="f-date">Date</label><input id="f-date" name="date" type="date"></div>' +
              '</div>' +
              '<div class="field"><label for="f-msg">What are the pictures for?</label>' +
                '<textarea id="f-msg" name="message" rows="4"></textarea></div>' +
              '<input type="text" name="_honey" style="display:none" tabindex="-1" autocomplete="off">' +
              '<button class="submit" type="submit">Send enquiry</button>' +
              '<p class="form-note" id="formNote">We reply within two working days.</p>' +
            '</form>' +
          '</div>' +
        '</div>' +
      '</div>',
      { hideCta: true }
    );
  }

  function viewMissing() {
    return shell(
      '<div class="wrap" style="padding-block:80px 120px">' +
        '<span class="eyebrow">404</span>' +
        '<h1 class="page-title" style="margin:14px 0 26px">Nothing here</h1>' +
        '<p class="prose"><a class="big-link" href="#/work">See the work instead</a></p>' +
      '</div>'
    );
  }

  /* ------------------------------------------------------------- carousel
     Drag-driven, after the reference: the whole stage is grabbable,
     the filmstrip follows the pointer 1:1 with transitions off, and on
     release it snaps to whichever label ends up nearest the centre.
     Dragging left advances; the background crossfades to match.
     ---------------------------------------------------------------------- */
  var carousel = null;

  function stopCarousel() {
    if (!carousel) return;
    clearTimeout(carousel.timer);
    window.removeEventListener('resize', carousel.onResize);
    carousel = null;
  }

  function startCarousel() {
    var track = $('#stripTrack');
    var stage = $('.home');
    if (!track || !stage) return;

    var n = PROJECTS.length;
    var REPS = 5;               // enough copies that a long drag never runs dry
    var AUTO = 6800;            // gentle autoplay, cancelled by the first touch
    var SNAP_VELOCITY = 0.45;   // px/ms past which a flick carries to the next

    var html = '';
    for (var r = 0; r < REPS; r++) {
      html += PROJECTS.map(function (p, i) {
        return '<a class="strip-item" data-i="' + i + '" draggable="false"' +
               ' href="#/work/' + p.slug + '">' +
          '<span class="t">' + esc(p.title) + '</span>' +
        '</a>';
      }).join('');
    }
    track.innerHTML = html;

    var items = $$('.strip-item', track);
    var MID = Math.floor(REPS / 2) * n;        // first item of the centre copy

    // Only the centre copy is real as far as assistive tech and Tab are
    // concerned; the others exist purely to give the track runway.
    items.forEach(function (el, k) {
      var real = k >= MID && k < MID + n;
      if (!real) { el.setAttribute('aria-hidden', 'true'); el.tabIndex = -1; }
    });
    var state = { i: 0, timer: null, onResize: null, engaged: false };
    carousel = state;

    /* translateX that puts item k under the centre of the viewport */
    function offsetFor(k) {
      var el = items[k];
      if (!el) return 0;
      var vp = track.parentElement.getBoundingClientRect().width;
      return vp / 2 - (el.offsetLeft + el.offsetWidth / 2);
    }

    function setX(x, live) {
      // `live` means the pointer is driving it: follow 1:1 with no easing.
      track.classList.toggle('dragging', !!live);
      track.style.transform = 'translate3d(' + x + 'px,0,0)';
    }

    function paint() {
      var p = PROJECTS[state.i];

      $$('.home-slide').forEach(function (s) {
        s.classList.toggle('is-active', +s.dataset.i === state.i);
      });
      items.forEach(function (el, k) {
        el.classList.toggle('is-active', (k % n) === state.i);
      });

      // The reference carries no caption at all, which is why its steps feel
      // instant. Ours keeps one, so it has to change on the same frame the
      // step begins — fading the old text out first is what read as lag.
      var cap = $('#homeCaption');
      if (cap) {
        $('.t', cap).textContent = p.title;
        $('.s', cap).textContent = p.blurb;
        cap.classList.add('in');
      }

      var num = $('#stripNum');
      if (num) num.textContent = (state.i + 1 < 10 ? '0' : '') + (state.i + 1);

      var bar = $('#stripBar');
      if (bar) bar.style.transform = 'scaleX(' + ((state.i + 1) / n) + ')';
    }

    /* Land on index i, then silently re-home to the centre copy so there is
       always runway in both directions. */
    function goTo(i) {
      state.i = ((i % n) + n) % n;
      setX(offsetFor(MID + state.i));
      paint();
      queueAuto();
    }

    function queueAuto() {
      clearTimeout(state.timer);
      if (state.engaged) return;             // user has taken over
      state.timer = setTimeout(function () { goTo(state.i + 1); }, AUTO);
    }

    function engage() {
      state.engaged = true;
      clearTimeout(state.timer);
    }

    /* ------------------------------------------------------------ dragging
       One gesture moves exactly one step. The track still follows the
       pointer so the drag feels physical, but where you let go only decides
       the direction, never how far — a long drag and a short flick both
       advance by one. Handlers sit on the whole stage rather than a bare
       surface underneath, so a drag that starts on top of a label works
       like any other. */
    var drag = null;
    var suppressClick = false;

    function pitchPx() {
      var gap = parseFloat(getComputedStyle(track).columnGap || 0) || 0;
      return items[MID].offsetWidth + gap;
    }

    stage.addEventListener('dragstart', function (e) { e.preventDefault(); });

    stage.addEventListener('pointerdown', function (e) {
      if (e.button !== undefined && e.button !== 0) return;
      drag = {
        id: e.pointerId,
        startX: e.clientX,
        baseX: offsetFor(MID + state.i),
        moved: false,
        lastX: e.clientX,
        lastT: e.timeStamp,
        v: 0
      };
      // Capture is deferred to the first real move (below), not taken here.
      // Capturing on every pointerdown — including a plain click — makes
      // the browser retarget the resulting click event to the stage instead
      // of whatever was actually under the pointer, so a tap on a strip
      // label's <a> never reaches its href. Only an actual drag needs capture.
    });

    stage.addEventListener('pointermove', function (e) {
      if (!drag || e.pointerId !== drag.id) return;
      var dx = e.clientX - drag.startX;
      if (!drag.moved && Math.abs(dx) < 4) return;
      if (!drag.moved) {
        drag.moved = true; engage(); stage.classList.add('dragging');
        try { stage.setPointerCapture(e.pointerId); } catch (err) {}
      }

      var dt = e.timeStamp - drag.lastT;
      if (dt > 0) drag.v = (e.clientX - drag.lastX) / dt;
      drag.lastX = e.clientX;
      drag.lastT = e.timeStamp;

      // resist past the pointer a little so a long drag does not imply distance
      setX(drag.baseX + dx * 0.55, true);
    });

    function endDrag(e) {
      if (!drag || (e && e.pointerId !== drag.id)) return;
      var d = drag;
      drag = null;
      stage.classList.remove('dragging');
      if (!d.moved) return;                    // a click, not a drag

      suppressClick = true;                    // swallow the click this drag ends on
      setTimeout(function () { suppressClick = false; }, 0);

      var dx = e.clientX - d.startX;
      var threshold = Math.min(52, pitchPx() * 0.16);
      var committed = Math.abs(dx) > threshold || Math.abs(d.v) > SNAP_VELOCITY;

      goTo(committed ? state.i + (dx < 0 ? 1 : -1) : state.i);
    }

    stage.addEventListener('pointerup', endDrag);
    stage.addEventListener('pointercancel', function () {
      if (!drag) return;
      drag = null;
      stage.classList.remove('dragging');
      goTo(state.i);                           // settle back, no step
    });

    /* Trackpad and wheel.
       A trackpad swipe is not one event — it is a burst of dozens, with
       momentum trailing for up to a second afterwards, and the axis is
       rarely clean. So: take whichever axis dominates each event, add it
       up, fire once when the total crosses a small threshold, and stay
       disarmed until the stream has been quiet for a moment. One swipe,
       one step, however messy the gesture. */
    var wheelAcc = 0, wheelIdle = null, wheelArmed = true;
    var WHEEL_TRIGGER = 40;   // px of travel before a step commits
    var WHEEL_QUIET = 170;    // ms of silence that ends a gesture

    stage.addEventListener('wheel', function (e) {
      var d = Math.abs(e.deltaX) >= Math.abs(e.deltaY) ? e.deltaX : e.deltaY;
      if (!d) return;
      e.preventDefault();

      wheelAcc += d;
      clearTimeout(wheelIdle);
      wheelIdle = setTimeout(function () { wheelAcc = 0; wheelArmed = true; }, WHEEL_QUIET);

      if (wheelArmed && Math.abs(wheelAcc) >= WHEEL_TRIGGER) {
        wheelArmed = false;
        engage();
        goTo(state.i + (wheelAcc > 0 ? 1 : -1));
        wheelAcc = 0;
      }
    }, { passive: false });

    /* Every label is a link, so a click just navigates — no centring step
       first. The only job here is to swallow the click that ends a drag. */
    items.forEach(function (el) {
      el.addEventListener('click', function (e) {
        if (suppressClick) { e.preventDefault(); return; }
        engage();
      });
    });

    // exposed so the arrow keys travel the same path as a drag
    state.step = function (d) { engage(); goTo(state.i + d); };

    state.onResize = function () { setX(offsetFor(MID + state.i)); };
    window.addEventListener('resize', state.onResize);

    // first paint without animation so the strip does not fly in
    setX(offsetFor(MID));
    goTo(0);
  }

  /* ----------------------------------------------------------------- gallery
     Shoot-page image viewer: one large crossfading frame, a thumbnail
     strip, and arrows — after the reference's project galleries. */
  var gallery = null;

  function stopGallery() { gallery = null; }

  function startGallery() {
    var root = $('#gallery');
    if (!root) return;
    var slides = $$('.gallery-slide', root);
    var thumbs = $$('.gallery-thumb', root);
    var n = slides.length;
    if (!n) return;

    var state = { i: 0 };
    gallery = state;

    function goTo(i) {
      state.i = ((i % n) + n) % n;
      slides.forEach(function (s) { s.classList.toggle('is-active', +s.dataset.i === state.i); });
      thumbs.forEach(function (t, k) { t.classList.toggle('is-active', k === state.i); });
      var num = $('#galleryNum', root);
      if (num) num.textContent = (state.i + 1 < 10 ? '0' : '') + (state.i + 1);
      var active = thumbs[state.i];
      if (active && active.scrollIntoView) {
        active.scrollIntoView({ block: 'nearest', inline: 'center', behavior: 'smooth' });
      }
    }

    var prev = $('.gallery-arrow.prev', root), next = $('.gallery-arrow.next', root);
    if (prev) prev.addEventListener('click', function () { goTo(state.i - 1); });
    if (next) next.addEventListener('click', function () { goTo(state.i + 1); });
    thumbs.forEach(function (t, k) { t.addEventListener('click', function () { goTo(k); }); });

    state.step = function (d) { goTo(state.i + d); };
    goTo(0);
  }

  /* --------------------------------------------------------------- reveal */
  var io = null;
  function bindReveals() {
    if (io) io.disconnect();
    var reduce = window.matchMedia && window.matchMedia('(prefers-reduced-motion: reduce)').matches;
    var els = $$('.reveal');
    if (reduce || !('IntersectionObserver' in window)) return;   // stay at rest, visible

    // Only things starting below the fold get hidden, so the first painted
    // frame of every route is already complete.
    var fold = window.innerHeight;
    var below = els.filter(function (el) {
      return el.getBoundingClientRect().top > fold * 0.92;
    });
    below.forEach(function (el) { el.classList.add('pending'); });

    io = new IntersectionObserver(function (entries) {
      entries.forEach(function (e) {
        if (e.isIntersecting) {
          e.target.classList.add('in');
          io.unobserve(e.target);
        }
      });
    }, { rootMargin: '0px 0px -8% 0px', threshold: 0.06 });
    below.forEach(function (el) { io.observe(el); });
  }

  /* --------------------------------------------------------------- router */
  function parse() {
    var h = (location.hash || '#/').replace(/^#/, '');
    var seg = h.split('/').filter(Boolean);
    if (!seg.length) return { name: 'home' };
    if (seg[0] === 'work') {
      if (!seg[1]) return { name: 'work' };
      if (seg[2]) return { name: 'shoot', cat: seg[1], slug: seg[2] };
      return { name: 'project', slug: seg[1] };
    }
    if (['studio', 'process', 'contact'].indexOf(seg[0]) > -1) return { name: seg[0] };
    return { name: '404' };
  }

  function syncNavTone() {
    var nav = $('.nav');
    if (!nav) return;
    var r = parse();
    var onPhoto = r.name === 'home' && !openPanel;
    nav.classList.toggle('on-photo', onPhoto);
    nav.classList.toggle('on-paper', !onPhoto);

    // Mark the current section in the drawer. A project page counts as Work.
    var section = (r.name === 'project' || r.name === 'shoot') ? 'work' : r.name;
    $$('.drawer-links a').forEach(function (a) {
      if (a.dataset.route === '#/' + section) a.setAttribute('aria-current', 'page');
      else a.removeAttribute('aria-current');
    });
  }

  var lastKey = null;

  function render(force) {
    var r = parse();
    var key = r.name + '|' + (r.cat || '') + '|' + (r.slug || '');
    var app = $('#app');
    // A JS-driven navigation renders straight away and the browser's own
    // hashchange arrives just after it. Without this guard the view would
    // build twice and lose its scroll position.
    if (!force && key === lastKey && app && app.children.length) return;
    lastKey = key;
    stopCarousel();
    stopGallery();

    var html;
    if (r.name === 'home') html = viewHome();
    else if (r.name === 'work') html = viewWork();
    else if (r.name === 'project') html = viewProject(r.slug);
    else if (r.name === 'shoot') html = viewShoot(r.cat, r.slug);
    else if (r.name === 'studio') html = viewStudio();
    else if (r.name === 'process') html = viewProcess();
    else if (r.name === 'contact') html = viewContact();
    else html = viewMissing();

    app.innerHTML = html;
    document.title = titleFor(r);
    window.scrollTo(0, 0);
    syncNavTone();

    if (r.name === 'home') startCarousel();
    if (r.name === 'shoot') startGallery();
    if (r.name === 'contact') bindForm();
    bindReveals();
  }

  function titleFor(r) {
    var base = 'Relentless Frames';
    if (r.name === 'home') return base;
    if (r.name === 'work') return 'Work — ' + base;
    if (r.name === 'project') { var p = bySlug(r.slug); return p ? p.title + ' — ' + base : 'Not found — ' + base; }
    if (r.name === 'shoot') { var sh = shootBySlug(r.slug); return sh ? sh.title + ' — ' + base : 'Not found — ' + base; }
    if (r.name === '404') return 'Not found — ' + base;
    return r.name.charAt(0).toUpperCase() + r.name.slice(1) + ' — ' + base;
  }


  function bindForm() {
    var f = $('#enquiry');
    if (!f) return;
    f.addEventListener('submit', function (e) {
      e.preventDefault();
      if (f.querySelector('[name="_honey"]').value) return;   // bot caught the honeypot

      var d = new FormData(f);
      var note = $('#formNote');
      var btn = f.querySelector('.submit');
      var mailBody = [
        'Name: ' + (d.get('name') || ''),
        'Email: ' + (d.get('email') || ''),
        'Commission type: ' + (d.get('type') || ''),
        'Date: ' + (d.get('date') || ''),
        '',
        d.get('message') || ''
      ].join('\n');
      var mailtoFallback = 'mailto:' + STUDIO.email +
        '?subject=' + encodeURIComponent('Commission enquiry — ' + (d.get('type') || '')) +
        '&body=' + encodeURIComponent(mailBody);

      btn.disabled = true;
      note.textContent = 'Sending…';

      fetch('https://formsubmit.co/ajax/' + STUDIO.email, {
        method: 'POST',
        headers: { 'Content-Type': 'application/json', 'Accept': 'application/json' },
        body: JSON.stringify({
          name: d.get('name') || '',
          email: d.get('email') || '',
          type: d.get('type') || '',
          date: d.get('date') || '',
          message: d.get('message') || '',
          _subject: 'Commission enquiry — ' + (d.get('type') || ''),
          _template: 'table'
        })
      }).then(function (res) {
        if (!res.ok) throw new Error('bad status');
        return res.json();
      }).then(function () {
        f.reset();
        note.textContent = 'Sent — thank you. We reply within two working days.';
      }).catch(function () {
        note.innerHTML = 'Could not send automatically. <a href="' + mailtoFallback + '">Email us directly</a> instead.';
      }).finally(function () {
        btn.disabled = false;
      });
    });
  }

  /* ----------------------------------------------------------------- boot */
  function boot() {
    buildChrome();

    $('#menuBtn').addEventListener('click', function () {
      setPanel(openPanel === 'menu' ? null : 'menu');
    });
    $('#searchBtn').addEventListener('click', function () {
      setPanel(openPanel === 'search' ? null : 'search');
    });
    $('#searchInput').addEventListener('input', function (e) { renderSearch(e.target.value); });

    /* Route from JS rather than trusting the anchor's default action. Inside
       a sandboxed iframe — which is how the hosted version of this site is
       embedded — that default can be swallowed, leaving the URL unchanged
       and the page looking dead on every click. */
    document.addEventListener('click', function (e) {
      if (e.defaultPrevented || e.button) return;
      if (e.metaKey || e.ctrlKey || e.shiftKey || e.altKey) return;   // let new-tab through
      var a = e.target && e.target.closest ? e.target.closest('a[href^="#/"]') : null;
      if (!a) return;

      e.preventDefault();
      var to = a.getAttribute('href');
      if (openPanel) setPanel(null);

      if (location.hash !== to) {
        try { location.hash = to; } catch (err) { /* sandboxed — carry on */ }
      }
      render();                        // never wait on the browser to route
      window.scrollTo(0, 0);
    });

    document.addEventListener('keydown', function (e) {
      if (e.key === 'Escape' && openPanel) { setPanel(null); return; }
      if (openPanel) return;
      var name = parse().name;

      if (name === 'home' && carousel) {
        if (e.key === 'ArrowRight') { e.preventDefault(); nudge(1); }
        if (e.key === 'ArrowLeft') { e.preventDefault(); nudge(-1); }
        if (e.key === 'Enter') { location.hash = '#/work/' + PROJECTS[carousel.i].slug; }
        return;
      }
      if (name === 'shoot' && gallery) {
        if (e.key === 'ArrowRight') { e.preventDefault(); gallery.step(1); }
        if (e.key === 'ArrowLeft') { e.preventDefault(); gallery.step(-1); }
      }
    });

    function nudge(d) {
      if (carousel && carousel.step) carousel.step(d);
    }

    window.addEventListener('hashchange', function () { render(); });
    window.addEventListener('popstate', function () { render(); });
    render(true);
  }

  var imagesReady = loadImages();
  function whenDomReady(cb) {
    if (document.readyState === 'loading') document.addEventListener('DOMContentLoaded', cb);
    else cb();
  }
  whenDomReady(function () { imagesReady.then(boot); });
})();
